# DESIGN.md — ツール_筋トレ記録

## 現在地（2026-08-18 時点 / 最初にここを読む）

**バージョン**：`v0.13.12` ／ `sw.js` の `CACHE` は `kintore-v56`

**いま追っている問題**：App Store提出前の実機検証。**通知が1件も配信されない**。

| 状態 | いつ確認したか |
|---|---|
| アプリを開いている間のアラーム音が鳴る | ✅ v0.13.0 / 実機。v0.13.4 実機でも確認 |
| 終了時に振動する | ✅ v0.13.4 / 実機（2026-08-17） |
| アプリを閉じていても通知バナーが出る | ✅ v0.13.1 / 実機（2026-08-11）→ **❌ v0.13.4 実機では出ない** |
| 開始直後に他アプリへ切り替えても通知が来る | ❌ v0.13.4 実機で**来ない**（音も鳴らない） |
| 前面で終わったときバナーが出る | ❌ v0.13.4 実機で**出ない** |
| 前面で音が二重に鳴らない | ✅ 二重にはならない（ただしバナー自体が出ていないので証拠としては弱い） |
| 消音スイッチONでも前面のアラーム音が鳴る | ✅ v0.13.7 / 実機（2026-08-17・トグルON/OFFいずれでも鳴る） |
| アプリの**アイコンにバッジ**が付く | ❌ 付かない。ただし `buildNotif()` が `badge` を渡していないので**元から付かない**（9.12-5） |

**v0.13.8 実機診断で特定した主原因**：`AlarmAudioPlugin` がiOSへ登録されていなかった。
`Main.storyboard` では `MainViewController` を指定していたが、`SceneDelegate` が起動時に
`CAPBridgeViewController()` を直接生成してStoryboardのViewControllerを上書きしていたため、
`MainViewController.capacitorDidLoad()` と `registerPluginInstance(AlarmAudioPlugin())` が実行されていなかった。
実機診断では、消音モード・iPhone側通知設定・通知音実ファイルの全経路が
`"AlarmAudio" plugin is not implemented on ios` で失敗した。

**v0.13.9 の修正**：`SceneDelegate` のrootを `MainViewController()` に変更し、
自前プラグインの登録経路を実際の起動経路へ一致させた。これにより消音モードON/OFF、
通知設定取得、通知音ファイル確認、設定アプリを開く処理が同時に復旧する。
なお `@capacitor/local-notifications@8.2.1` のiOS実装は通知スキーマの `badge` 値を
`UNMutableNotificationContent.badge` へ設定する処理を持たないため、JSへ `badge: 1` を足すだけでは直らない。
バックグラウンド時のアイコンバッジは通知表示の復旧後に別途対応する。

**v0.13.9 実機結果**：自前プラグイン登録、消音モードON/OFF、前面アラーム、
バックグラウンド通知がすべて正常動作することを確認した。

**v0.13.10 の修正**：バックグラウンド通知の配信時にアイコンバッジを1へ設定する。
JSの通知データへ `badge: 1` を追加し、`@capacitor/local-notifications@8.2.1` のiOS実装が
その値を `UNMutableNotificationContent.badge` へ反映する最小パッチを `build-ios.js` で適用する。
`node_modules` はCodemagicの `npm install` で毎回再生成されるため、生成後に必ず走る
ビルドスクリプトへ入れた。既知の差し込み位置が見つからなければビルドを停止する。
さらに、通知が付けたバッジをアプリ起動・復帰時に確実に消すため、`AlarmAudioPlugin` に
`setBadge(value)` を追加した。iOS 16以降は `UNUserNotificationCenter.setBadgeCount`、
それ以前は `UIApplication.applicationIconBadgeNumber` を使い、Web Badging APIは予備経路とする。
通常の初期化時にも `clearBadge()` を呼び、通知からのコールド起動で
`visibilitychange` が発火しない場合もバッジを残さない。

**v0.13.11 の追加**：設定画面の「サポート」と「プライバシー」の間に「応援」を追加した。
「レビューで応援する」を押すと、Apple ID `6800222286` のApp Storeレビュー入力画面を
`action=write-review` 付きの商品URLで開く。iOS版は自前プラグインからApp Storeを開き、
PWA版は新しいタブで同じページを開く。

**v0.13.12 の修正**：レビューリンクの商品IDを `6800222286` へ訂正した。
以前記載していた `1049236802` はApp Store Connectのアカウント表示に含まれる番号で、
筋トレLogの商品IDではなかった。App Store Connectの筋トレLog詳細URL
`/apps/6800222286/distribution` で実際の商品IDを確認した。

**v0.13.4 実機の結果（2026-08-17）**：JS もネイティブプラグインも生きている
（アプリ内アラーム音・振動が動く＝ `AlarmAudioPlugin` は登録されている）のに、
**前面・背面を問わずローカル通知が1件も出ない**。
前面表示だけを決める `presentationOptions` では背面で出ないことを説明できないため、
残る原因は「OSに予約が入っていない」か「許可・iPhone側の通知設定で止められている」のどちらか。

**v0.13.5 でやったこと（9.9）**：原因が画面のどこにも出ないまま
「開始して5分待ったが鳴らなかった」を繰り返していたので、**計器を表に出し、直せるようにした**。

**v0.13.6 でやったこと（9.10）**：その計器を外部レビューにかけたところ、
**計器自体が嘘をつく状態だった**ことが分かったので直した。要点は3つ。

- 「通知の予約」欄の **OK は、OSに予約が入ったことを確認してから**書くようにした
  （それまでは `schedule()` が返っただけで OK と書いていた。Capacitor はOSへの登録完了を待たずに返す）
- 「アプリを開いている時の通知」欄を新設し、**前面の通知音をアプリ側で意図的に切っている**ことを明示した
- 診断の取得に失敗したとき、**前回の正常値が残らない**ようにした（古い `granted` が出続けていた）

**v0.13.7 でやったこと（9.11）**：v0.13.6 を再度レビューにかけ、**計器がまだ嘘をつき得る2か所**を直した。

- 予約の確認を **idだけでなく「今回の時刻」でも突き合わせる**ようにした
  （同じidで置き換える作りなので、前回の予約が残っているだけでもidは一致し、
  ＋30秒などで置き換えが失敗していても `OK` と出ていた）
- 診断の各欄に **欄ごとの世代番号**を持たせ、追い越された問い合わせは
  成功・失敗・時間切れのどれでも書き込まないようにした
  （古い問い合わせの4秒の時間切れが、新しく取れた正しい値を消していた）

**v0.13.7 実機の結果（2026-08-17）**：症状は v0.13.4 から変わらず、**アプリを閉じていると通知が来ない**。
前面のアラーム音と振動は消音スイッチの状態にかかわらず鳴る。
加えて**タイマー画面の見出し直下に、中身が空の赤い枠**が出ていた。

**v0.13.8 でやったこと（9.12）**：その空の赤枠の正体は、**`hidden` 属性がCSSに打ち消されていた**こと。
`.notif-warn { display: block }` がUA既定の `[hidden]{display:none}` に勝つため、
警告が無くても枠だけが出続けていた。`[hidden]{display:none !important}` を1か所入れて全要素に効かせた。

- 同じ形の穴が `#rowIgnoreSilent`（`.s-row{display:flex}`）にもあり、**PWA版でiOS専用の行が見えていた**
- テストが `el.hidden`（属性）で判定していたため8モードすべてが素通りしていた → **実際に矩形が出ているか**で見る形に変えた
- **「赤枠が空だから正常」とは読めない**。空になる条件にはシステム通知OFFも含まれる（判定表は 9.12-4）

**Codemagic の注意**：ビルド開始時のブランチ既定値が `master` になっているが、
このリポジトリは **`main`**。`master` のままだと `codemagic.yaml not found` で `Start new build` が押せない。

### 次にやること（この順で）

1. **Codemagic で再ビルド**（ブランチに **`main`** を選ぶ。手順は 7-14）
2. TestFlight で入れたら、**設定画面の一番下が `筋トレLog v0.13.8 — Phase 8` になっていること**を先に確認する
   （前回「古い版でテストしていた」取り違えが実際に起きているため、毎回ここから始める）
3. **タイマー設定の「システム通知」がONか**を先に見る。OFFなら通知は予約すらされない
   （このとき警告帯は出ないので、画面からは正常と区別がつかない。9.12-4）
4. **タイマー画面を開くだけ**で、上部に赤い警告が出ていないか見る。出ていたらそれが原因なので、
   警告をタップ → iPhone の設定が開く → 指示どおり切り替えて戻る
5. 警告が出ない状態でタイマーを開始 → すぐホーム画面へ戻る → 通知が来るか
6. それでも来ないときは「設定 > タイマー > 音の状態」の次の3行を読む（**判定表は 9.10-5**）
   - **通知の予約** ／ **OSに入っている予約** ／ **配信された通知**
   - 「通知の予約」は v0.13.6 から **OSに確認が取れてはじめて `OK（OS確認済み）`** になり、
     v0.13.7 からは **その確認に予約時刻の一致まで含む**。
     `OK` なのに鳴らないなら、原因は予約ではなく配信・表示の側にある
7. 通ったら残りの実機検証2件（ライトモードのステータスバー可読性 / 機種変更時の復元）→ App Store Connect のメタデータ・審査提出へ

**バージョンを上げるときに触る6か所**：`index.html` の `?v=`（css 1 + js 3 の計4か所）と `.version` 表記、`sw.js` の `CACHE`。
**iOSビルド前に必ず**：`node --check js/app.js` → `node build-ios.js` → `npx cap sync ios`。

## 概要・背景

iPhoneで使う筋トレ記録アプリ。**PWA（Webアプリ）方式**で完全無料で開発・公開する。
将来的にApp Store公開（Capacitorでラップ、Apple Developer 年間12,980円）へ発展可能な構成。

- 開発環境：Windows 11（ローカルでHTML確認 → GitHub Pagesで公開 → iPhone実機確認）
- 利用シーン：ジムで片手操作。オフラインでも記録できること。
- ユーザー確認済み方針：まずPWA → 将来App Store（2026-07-08 決定）

## 決定事項（重要）

| 項目 | 決定 | 理由 |
|---|---|---|
| 公開方法 | GitHub Pages（無料） | リポジトリはpublicになる |
| 公開先URL | https://kanaeru-apps.github.io/kintore-log/ （リポジトリ `kanaeru-apps/kintore-log`） | 2026-07-26に個人アカウントから組織アカウントへ移設。URL・コミット履歴から開発者の実名が見えないようにするため（下記 Phase 7-0） |
| データ保存 | 端末内（localStorage）＋ Googleスプレッドシート二重保存 | Phase 4でGAS連携 |
| ローカルDB | localStorage（抽象化レイヤー js/db.js 経由） | 記録データは軽量（数年分でも数百KB）。file://でも動作し確実。将来IndexedDBへ移行可能な構造 |
| GAS URLの扱い | **コードに埋め込まない**。アプリ内設定画面で入力し端末内にのみ保存 | publicリポジトリに秘密情報を置かないため |
| グラフ | 外部ライブラリ不使用・自作SVG（Phase 3で決定・実装） | オフラインで確実に動く／ダーク・ボルトイエロー配色に完全に合わせられる／既存の人体図・RMダイヤルと同じ方式 |
| 推定1RM | Epley式：1RM = 重量 × (1 + 回数÷30) | |
| ボリューム定義 | Σ(重量 × 回数) | 部位別・期間別レポートの基礎数値 |

## Phase 構成と進捗

- [x] **Phase 1**：記録機能＋PWA基盤（2026-07-08 実装）
  - 種目マスタ（部位別、初期リスト内蔵、追加・名称変更・削除）
  - ワークアウト記録（日付・種目・重量×回数のセット記録、±ステッパー入力）
  - 前回記録の参考表示・前回値の自動プリフィル
  - コンディション（5段階）・日メモ
  - 履歴閲覧（月別グルーピング、展開表示、日削除）
  - CSVエクスポート（UTF-8 BOM付き、Excel対応）
  - PWA基盤（manifest.json / sw.js ※Service WorkerはHTTPS公開後に有効化される）
- [x] **Phase 1.1**：ユーザーフィードバック反映（2026-07-09 実装 / v0.1.1）
  - 部位に「有酸素」追加（初期種目：トレッドミル・エアロバイク・ランニング・ウォーキング）
  - 種目に**器具**フィールド追加（バーベル/ダンベル/マシン/ケーブル/自重）。同名種目を器具ごとに別登録できる
  - 種目選択シートは部位内で器具ごとにグルーピング表示
  - 種目追加時（初回）は**3セット分**の行を最初から表示。前回記録があれば全セット引き継ぎ
  - 日メモのプレースホルダーを「メモ（自由記載）」に変更
  - CSVに「器具」列追加
  - データマイグレーション（v1→v2）：既存データに equip フィールドを自動付与
- [x] **Phase 1.2**：種目情報モーダル（2026-07-09 実装 / v0.1.2）
  - 種目名タップ（記録カード・種目選択シートのⓘ・設定の種目名）で情報モーダルを表示
  - 自作SVG人体図（前面/背面）で鍛えられる部位をハイライト（著作権フリー・オフライン対応）
  - 「▶ 動きを見る」ボタン：登録済み参考動画URLがあればそれを開く。なければ「(器具) 種目名 やり方」のYouTube検索結果を開く（APIキー不要）
  - 参考動画URLの登録（種目マスタの `video` フィールド）・フォームメモ（`note` フィールド）。入力即保存
- [x] **Phase 1.3**：記録画面ヘッダー改修（2026-07-09 実装 / v0.1.3）
  - 日付表示を「2026/07/09 (木)」形式（西暦・ゼロ埋め・曜日）に変更
  - **コンディション5段階を廃止**（UI・履歴表示・CSV列から削除。過去データのconditionフィールドは無害なので残置）
  - 日次サマリータイル4項目を追加：合計種目数・合計セット数・合計レップ数・合計負荷量(kg)
    - 重量か回数が入力されたセットのみ「実施」としてカウント（空の初期3行は数えない）
  - 入力時の再描画でカードのフェードインが再生されるチラつきを修正（アニメーションは日付/タブ切り替え時のみ）
- [x] **Phase 1.4**：RM計算機（2026-07-09 実装 / v0.1.4）
  - 下部タブバーに「計算機」タブ追加（記録・履歴・グラフ・タイマー・計算機・設定 の6タブ）
  - 重量：**左右ドラッグのダイヤル式**（目盛りルーラー、0.1kg刻み、0〜300kg、中央ポインタ）
  - レップス：−/＋ボタンで増減（1〜30）
  - 1RM換算表：1〜12 Reps の重量をリアルタイム表示。1〜3=筋力アップ、4〜12=筋肥大のカテゴリ表示。現在のレップス行をハイライト
  - 換算式：1RM = 重量 × (1 + 0.0244 × (回数 − 1))（参考アプリの計算値と一致することをテストで確認済み）
  - 実装：ダイヤルは横スクロール要素のscrollLeft→重量変換（iOSの慣性スクロールがそのまま効く）
  - **Phase 1.4.1（2026-07-10 / v0.2.3）**：目盛りを1kg＋0.5kg刻みに変更（従来の0.1kg=6px刻みは高DPR＋小数スクロールでモアレ「変な棒」が出ていた）。`.dial-scroll` に `overflow-y: hidden`＋`overscroll-behavior: contain` を追加し、縦スクロール/バウンスで数字が消える問題を修正（横スクロール限定に）
- [x] **Phase 1.5**：重量ドラムロールピッカー（2026-07-09 実装 / v0.1.5）
  - 記録画面の重量セルを、数字タップで開く**ドラムロール（ホイールピッカー）**式に変更
  - 上=軽い／下=重いの並び（0〜300kg・0.5kg刻み）。中央の選択値を黄色ハイライト＋kg単位表示
  - タッチはネイティブの慣性スクロール＋スクロールスナップ、マウスは上下ドラッグ／ホイールに対応
  - 「完了」で確定・「キャンセル」/背景タップで据え置き。±ボタン（2.5kg刻み）も従来どおり併存
  - 実装：`.drum-scroll`（scroll-snap-type: y mandatory）＋ scrollTop→index変換。項目高さ44pxはCSSと定数`DRUM_ITEM_H`で一致必須
- [x] **Phase 1.6**：種目の並べ替え（2026-07-09 実装 / v0.1.6）
  - 種目選択シートの各行に三本線グリップ（≡）を追加
  - グリップを**長押し**すると並べ替えモードに入り、ドラッグで順序を入れ替え可能（同じ器具グループ内で移動）
  - 「完了」ボタンで並べ替えモード終了。並べ替え結果は `state.exercises` に保存され永続化
  - 実装：`DB.reorderWithinPart(part, orderedIds)`で該当部位のみ再配置（他部位は保持）。ドラッグは pointer events + `elementFromPoint` によるライブ入れ替え
- [x] **Phase 1.7**：有酸素の記録項目対応（2026-07-10 実装 / v0.1.8）
  - 有酸素（部位=有酸素）の種目カードを、重量kg×回数ではなく**6項目**（時間(分)・距離(km)・速度(km/h)・傾斜(%)・カロリー(kcal)・心拍(bpm)）の2列グリッド入力に切り替え
  - 切り替え判定は記録カードに保存済みの `part === '有酸素'`（種目マスタ削除後も履歴が壊れない現行方式を踏襲）
  - 有酸素は「＋ セッション追加」で複数行を記録可能（インターバル対応）。新規追加時の既定行数は1（筋トレは3のまま）
  - 有酸素カードでは kg/回数/ドラムロール/±ステッパー/VOL(kg)を出さず、フットは「計 ○分 · ○km」の集計表示
  - 前回参考表示・履歴明細も有酸素向け（時間/距離/カロリー）に整形
  - 日次サマリータイルを内容のある種類だけ表示（筋トレ=レップ/負荷量、有酸素=時間/距離）。両方ある日は自動折り返し
  - CSVに有酸素6項目の列を追加（`時間min,距離km,速度kmh,傾斜%,カロリーkcal,心拍bpm`）
  - **注意**：本改修より前に有酸素種目を「kg×回数」で記録していた場合、その日は新レイアウトになり旧値は非表示（データ自体は残存）
- [x] **Phase 1.7.1**：有酸素グリッドのはみ出し修正（2026-07-10 / v0.1.9）
  - 有酸素の2列グリッドが `grid-template-columns: 1fr 1fr` だと `1fr(=minmax(auto,1fr))` の auto 最小値が数値入力の中身に引きずられ、右カラム（距離/傾斜/心拍）がカード外へはみ出していた
  - `minmax(0, 1fr)` ＋ `.cf { min-width: 0 }` でカラムを確実に縮小させて解消（Chromeヘッドレスで実測・スクショ確認済み。360px幅で内部オーバーフロー0）
- [x] **Phase 2（タイマー）**：インターバルタイマー（2026-07-10 実装 / v0.2.0）
  - プリセット 1分/3分/5分（タップで即スタート）＋カスタムホイール（1分刻み・最大15分、重量ドラムと同じ上下スクロール操作）。カスタム値はlocalStorage(`kintore_timer_min`)に保存
  - カウントダウンは**終了時刻の実時刻(endAt)ベース** → バックグラウンド復帰時に経過を自己補正（iOSでJSが止まっても復帰時に正しく反映／終了済みなら終了処理）
  - 円形リング表示、操作は 一時停止/再開・−30秒・＋30秒・リセット（v0.2.2で−30秒を＋30秒の左に追加。−30で残り0以下なら終了扱い）。カスタムのスタートボタンは「START」（v0.2.2）。終了後は**1回で停止**（「もう一度」で同じ時間を再スタート）
  - 終了時の知らせ（v0.2.1）：終了表示「**TIME UP**」＋ビープ音**約5秒**（Web Audio・0.5秒間隔で11回・タップ時にアンロック）＋OS通知（許可時・iOS16.4+のPWA）＋アプリバッジ（`setAppBadge`）＋バイブ約5秒（`navigator.vibrate`・**対応端末のみ／iPhoneのSafari・PWAは非対応で無効**）＋作動中は画面スリープ抑止（Wake Lock）。リセット/再スタートで音・バイブを停止
  - **iOS制約（既知）**：画面ロック中やアプリ終了中はJS/タイマーが停止するため、ロック中の確実な通知発火はWeb単体では不可（サーバー経由のWeb Pushが必要＝将来課題）。「アプリを開いている間」は確実に作動
- [x] **Phase 2（タイマー設定）**：設定画面にタイマーの下層ページを追加（2026-07-11 実装 / v0.2.5）
  - 設定タブ「通知」セクションに「タイマー」行を追加。タップで下層のタイマー設定ページに遷移（`view-settings-timer`、専用のopenTimerSettings/closeTimerSettingsで表示切替。タブバーの状態には影響しない）
  - アラーム音を5種類から選択可能：ビープ（既定）・ベル・チャイム・電子音・ソフト（すべてWeb Audioのオシレーターで合成。音声ファイルなしでオフライン動作）。タップで即選択＋試聴（短いプレビュー）
  - 「タイマーの音を出す」「バイブレーションをする」「システム通知をする」をそれぞれトグルスイッチでオン/オフ可能（自作CSSスイッチ、`input:checked`で見た目切替）
  - 選択・トグル状態はlocalStorageに保存（`kintore_timer_sound` / `kintore_timer_sound_on` / `kintore_timer_vibrate_on` / `kintore_timer_notify_on`）し次回起動時も復元。既定はすべてオン・音はビープ（従来動作を変えない）
  - トグルオフの項目はタイマー終了時にスキップ（音を鳴らさない/振動しない/通知を出さない）。アプリバッジは対象外で常時付与
- [x] **種目管理：部位の折りたたみ（2026-07-11 実装 / v0.2.6）**
  - 設定タブ「種目管理」の部位見出しをタップ式に変更。初期状態は全部位が閉じており、件数バッジ（例「胸 5件」）のみ表示
  - 部位見出しをタップすると、その部位の種目リストだけが開閉する（`ui.exExpanded`で状態保持）
  - 新規種目を追加すると、選んだ部位だけが自動で開き（他の部位は閉じる）、追加した種目がその場で確認できる
- [x] **種目の完全重複を防止（2026-07-11 実装 / v0.2.7）**
  - `DB.findExercise(name, part, equip)` で部位・種目名・器具がすべて一致する既存種目を検出
  - 種目管理フォーム・種目選択シートの「作成して追加」の両方で、追加前に重複チェック。完全一致の場合はトーストでエラー表示し登録しない（器具違い・部位違いは別種目として登録可）
- [x] **種目管理：部位内を種目名のあいうえお順に整列（2026-07-11 実装 / v0.2.8）**
  - 従来は部位内が器具順（バーベル→ダンベル→マシン…）で並んでいたため、同名種目でも器具が違うと離れて表示されていた（例：チェストプレス(バーベル)とチェストプレス(マシン)が別の場所に分かれる）
  - `renderSettings()` の並び替えを「種目名のあいうえお順（`localeCompare(name, 'ja')`）→ 同名なら器具順」に変更。同名種目が必ず隣接して表示される
- [ ] **Phase 2（メニュー）**：メニューテンプレート（未着手）
- [x] **Phase 3**：分析（2026-07-11 実装 / v0.3.0）
  - グラフタブに「種目別」「部位別ボリューム」の2サブタブを追加。新規ファイル `js/charts.js`（外部ライブラリ不使用・自作SVGで折れ線/棒/積み上げ棒を描画）
  - 種目別：部位ごとにグループ化したドロップダウンで種目を選択。筋トレ種目は**推定1RM推移**（折れ線・計算機タブと同じEpley式）＋**セッション合計ボリューム**（棒グラフ）を表示、有酸素種目は**時間の推移**＋**距離の推移**を表示。直近12回分に絞って表示
  - 部位別ボリューム：週次（直近8週間）／月次（直近6ヶ月）を切り替え可能な積み上げ棒グラフ。部位ごとの色は記録画面の部位チップ（`.p-xxx`）と同じ配色を再利用。有酸素は重量ボリュームの概念が無いため対象外。データが無い期間も0として表示し空白期間が分かるようにする
  - 記録が無い種目・部位は「まだ記録がありません」等の空メッセージを表示
- [x] **記録UXの改善（2026-07-12 実装 / v0.3.1）**：5件のフィードバック対応
  1. **新規重量は50kgデフォルト**：`emptySet`（部位=筋トレ）が`{w:'',r:''}`→`{w:50,r:0}`に。初回3セットの穴埋めは`copySet`経由で50が連鎖するため全行50に揃う
  2. **セット追加は直前セットの重量を引き継ぎ、回数は常に0**：`copySet(part,s)`（筋トレ）が`r`を常に`0`に固定（前回セッションからの引き継ぎ・同日内の＋セット追加の両方に適用）。「前回」参考表示（`prevLine`）は`DB.prevRecord`の生データを見るため実際の履歴どおり正しく表示され続ける
  3. **重量ドラムに直接入力欄を追加**：`#drumDirectInput`。入力→ホイールへスクロール同期、ホイール操作→入力欄へ反映（フォーカス中は上書きしない）。ドラムの空欄時デフォルトも20→50kgに統一
  4. **有酸素の時間を時/分/秒の3列ホイール＋直接入力に変更**：新しい`#ctimeSheet`（重量ドラムと同じ開閉方式）。時=0〜5・分秒=0〜59。db.jsに`ts`（秒）フィールドを追加（`CARDIO_KEYS`に追加、既存データとの後方互換は`cp()`のnullish処理で自動対応・マイグレーション不要）。有酸素カードの時間セルは入力ボックスから「1時間05分30秒」等を表示するボタンに変更（`fmtCardioTime`）。CSVに「時間秒」列を追加
  5. **「セッション合計ボリューム」の呼称確認**：ユーザーの想定（全期間の総合計）とは異なり、実際は「1回（1日）のセッション単体の合計」を指すため、誤解を避けるためラベルは変更せず据え置き（ユーザーへ意味を説明済み）
- [x] **グラフ・レポートの構成見直し（2026-07-12 実装 / v0.3.2）**
  - 部位別ボリュームの「週次/月次」切り替えトグルを廃止し、**週次グラフの直下に月次グラフを常時両方表示**（`renderVolumePane`が週次・月次の2枚のカードを縦に並べて描画。`volumeCard(range,title)`で共通化）
  - グラフタブの最上部（種目別/部位別ボリュームのサブタブより上）に**総合計ボリューム**（全期間・全種目・全部位を合算、有酸素は対象外）の大きな表示を追加。`Charts.init()`のたびに再計算
- [x] **グラフの絞り込み・日次追加（2026-07-12 実装 / v0.3.3）**
  - 部位別ボリュームに**日次（直近8日間）**グラフを追加し、日次→週次→月次の順に縦に並べて表示（`dayBuckets(count)`＋`volumeByPartBuckets('day')`）
  - 種目別グラフの種目選択を、単一の巨大なselect（全部位の種目を1つのプルダウンで羅列）から、**まず部位チップを選び、その部位の種目だけが短い一覧で出る**方式に変更（`#chartPartChips`→`#chartExList`）。スクロールが長くなる問題を解消。部位・種目とも未選択時は自動的に先頭を選択
- [x] **種目選択をドラムロール式に変更（2026-07-12 実装 / v0.3.4）**
  - 上記v0.3.3のボタン一覧（`#chartExList`）は種目数が増えると縦に長くなる問題が残っていたため、重量ドラムと同じ**上下スクロールのホイールで1つ選ぶ**方式（`#chartExScroll`/`#chartExWheel`）に置き換え。3項目分（132px）の高さで常に一定、部位を切り替えても縦の長さは変わらない
  - スクロール確定（scroll-snap）ごとに`renderExercisePane()`を呼び即座にグラフを更新。モーダルの重量ドラムと違い確定ボタンは無く、選んだ瞬間に反映されるライブ更新方式（RM計算機の重量ダイヤルと同じ考え方）
- [x] **総合計ボリュームをトレーニング実施カレンダーに置き換え（2026-07-12 実装 / v0.4.0）**
  - グラフタブ最上部の「総合計ボリューム」表示を廃止し、代わりに**トレーニング実施カレンダー**（月表示・前月/次月ナビゲーション付き）を追加
  - カレンダー専用の部位チップ（ALL＋各部位。種目登録がある部位のみ表示）で絞り込み可能。ALLは記録した全ての日を、特定の部位を選ぶとその部位を実施した日だけを丸で色付け表示（色は部位チップ・部位別ボリュームと同じ配色`PART_COLOR`、ALLはボルトイエロー）
  - 実施判定は`isEntryFilled(e)`（app.jsの`filledSets`相当のロジックをcharts.js側に実装、重量×回数 or 有酸素6項目のいずれかが入力済みか）。`trainedDates(part)`が対象部位の実施日集合を返す
  - 状態は`state.calPart`/`state.calYear`/`state.calMonth`としてセッション中保持（タブ切替をまたいでも表示月・選択部位を記憶）
- [x] **記録タブの日付選択をカレンダー化（2026-07-12 実装 / v0.4.1）**
  - 記録タブの日付タップ時の挙動を、ブラウザ標準の`<input type="date">`（実施日の目印なし）から、グラフタブと同じ見た目のカレンダーシート（`#dateCalSheet`）に変更。常時埋め込みではなく**タップ時にシート表示**する方式を採用（記録タブは種目カード優先でスペースを圧迫しないため）
  - 部位フィルタは無く「何かしら記録した日」を単色（ボルトイエロー）で丸表示（`trainedDatesAny()`＝app.js内の`filledSets`を直接利用、charts.js側の実装とは独立）。日付セルをタップするとその日にジャンプしてシートが閉じる。「今日」ボタンで即座に本日へ
  - 選択中の日は`.cal-day.selected`（ボルトイエローの輪郭）で表示。今回`.cal-day.trained`に既定の背景色（ボルトイエロー）を追加（従来は文字色のみでグラフ側のインラインstyle任せだったため、単色表示のこのカレンダーでは無色に沈んで見えるバグがあった）
  - 不要になったネイティブ`<input type="date">`（`#datePicker`）と関連CSS（`.hidden-input`）を削除
- [x] **グラフタブの見出しを分離（2026-07-12 実装 / v0.4.2）**
  - ページ最上部の見出しを「グラフ・レポート」→**「ワークアウトデイ」**に変更（カレンダー部分の見出しとして）
  - 種目別/部位別ボリュームのタブの直前に、新たに**「グラフ・レポート」**の見出しを追加（`.chart-section-header`。ページ先頭用のセーフエリア分の余白は持たせず、`padding:10px 0 6px`の控えめな間隔にした専用スタイル）
- [x] **タイマー通知・ベスト記録ランキング・メダル・数値キーボードロック（2026-07-12 実装 / v0.5.0）**：4件のフィードバック対応
  1. **タイマー通知がバックグラウンドで発火しない問題の改善**：従来は200ms間隔の`setInterval`ポーリングで終了を検知していたため、他アプリに切り替えてタブがバックグラウンドになるとブラウザにポーリングが止められ、`finishTimer()`（通知発火元）が呼ばれないままだった。終了判定を専用の`setTimeout`（終了時刻ちょうどに1回だけ予約）に分離し、`startTick`/`pauseResumeTimer`/`addTime`のすべてで再スケジュールするように変更。`finishTimer()`に`if (timer.finished) return;`の二重発火防止ガードも追加。ただし画面ロック中やブラウザ完全終了時までは保証できない（本当の意味でのバックグラウンド配信にはWeb Pushサーバーが必要で、今回は見送り。ユーザーへの説明・承認済み）
  2. **種目別ベスト記録TOP3ランキング**：`DB.bestRecords(exId, topN)`/`DB.rankOnDate(exId, date)`を新設（同じ種目・同じ日の全セットの「重量×回数」を合算した値＝既存の「VOL」表示と同じ基準で、日付ごとに上位3件を算出）。グラフ・レポートの種目別タブ、推定1RM推移の上に「ベスト記録 TOP3（合計ボリューム）」カードとして🥇🥈🥉＋日付＋合計ボリュームを表示（`bestRecordCard()`）
  3. **記録時のメダルトースト**：重量ドラムの確定・±ステッパー（w/r）・直接入力（reps）のいずれで記録を更新した際も`checkRecordToast(entryId)`を呼び、その日の合計ボリュームがTOP3入りしていれば既存のトースト機構で「🥇 ○○ 自己ベスト更新！(1位)」等を表示。同じ順位が続く間は再発火しない（種目×日付ごとに直近表示した順位を`recordToastShown`で記憶し、順位が変わった時だけ再表示）
  4. **重量・回数などの数値入力欄をキーボードロック強化**：`type="number"`は端末・キーボードアプリによって数字以外の入力方式に切り替えられる余地があるため、`type="text"`＋`inputmode`（整数は`numeric`＋`pattern="[0-9]*"`、小数可の項目は`decimal`）に統一（重量直接入力・回数・カロリー系のカーソル項目・有酸素の時/分/秒）。合わせて不要になった数値スピナー非表示CSS（`input[type='number']`向け）を削除。なお`inputmode`はあくまでOSへのヒントであり、ユーザーが手動でキーボードを切り替える操作自体は防げない
  - jsdomでランキング計算・トースト発火/抑止・入力属性・タイマーのsetTimeout発火と冪等性ガードを検証（計30アサーション）、Chrome headlessでグラフのベスト記録カードと記録タブの新入力欄を目視確認
- [x] **Phase 4：Googleスプレッドシート同期（2026-07-13 実装 / v0.6.0）**
  - **同期方向・タイミング（決定事項）**：端末→スプレッドシートの一方向のみ（シート側の手編集はアプリに反映されない。誤操作でデータが壊れる心配がない）。同期は自動送信せず「今すぐバックアップ」ボタンを押した時だけ手動送信。送信粒度は前回同期以降に変更があった日だけの差分送信（`DB.dirtyDates()`）
  - **一般公開時の配慮**：設定画面には常時表示しない。非エンジニアの一般ユーザーがGASのセットアップという専門的な手順に迷わないよう、設定画面最下部の「バージョン表示」を7回連続タップした時だけ「クラウド同期」セクションが出現する隠しトリガー方式（`onVersionTap`）。解除状態は`localStorage`（`kintore_sync_unlocked`）にこの端末限定で保存され、以後は毎回表示される。なおリポジトリはpublicなためソースを読めばトリガーの仕組み自体は分かる＝UI上の配慮であり秘匿ではない、という前提をユーザーと確認済み
  - **db.js**：`state.dirtyDates`（日付→true のマップ）を追加。`addEntry`/`removeEntry`/`addSet`/`updateSet`/`removeSet`/`setMemo`/`deleteWorkout`のすべてで対象日をdirty化。導入前からの既存データは初回読み込み時に全記録日を自動でdirty化し、初回バックアップで全履歴を送れるようにする。`DB.dirtyDates()`/`DB.clearDirty(dates)`を公開
  - **app.js**：CSVエクスポートとスプレッドシート同期で同じ行データ（17列）を使うよう`rowsForDate(date)`に共通化（`exportCSV`もこれ経由に refactor、出力内容は変更なしをテストで確認）。GAS Web AppのURLは`localStorage`（`kintore_gas_url`）にのみ保存しコードには一切埋め込まない。送信は`fetch`＋`Content-Type: text/plain;charset=utf-8`で行う（GASのWeb AppはCORSプリフライト(OPTIONS)に応答しないため、プリフライトが発生しないtext/plainで送りJSON文字列はGAS側で`e.postData.contents`から読む、という定番の回避策）。送信成功で`DB.clearDirty`＋最終同期時刻を保存、失敗時はdirty状態を維持し次回の手動バックアップで再送されるようにする
  - **gas/code.gs（新規）**：ユーザー自身のスプレッドシートにコピペするテンプレ。`doPost`で送られてきた日付群の既存行を削除してから新しい行を追記（＝日付単位の上書き。セット編集・削除も正しく反映される）、最後に日付列でソート。日付列は`setNumberFormat('@')`で文字列固定にしてSheetsが日付型に自動変換するのを防止。ファイル冒頭にセットアップ手順（スプレッドシートでApps Script起動→貼り付け→ウェブアプリとしてデプロイ→URLを設定画面に貼る）をコメントで記載
  - **実機検証済み（2026-07-13）**：ユーザー自身のスプレッドシート「筋トレ記録_アプリバックアップ」にgas/code.gsをデプロイし、実際に「今すぐバックアップ」から同期成功を確認。デプロイ時に「アクセスできるユーザー」が既定の「自分のみ」だとGitHub Pages側からの匿名アクセスがGoogleログイン画面にリダイレクトされてしまう問題が発生 → 「全員」に変更して解決（「次のユーザーとして実行：自分」はそのままでよい）。あわせて初回デプロイ時に「This app is blocked」が出た場合はGASプロジェクトにGCPプロジェクト番号が未設定なことが原因（プロジェクトの設定→GCPプロジェクトを新規作成・紐付け→OAuth同意画面でテストユーザー登録、で解消）
  - GAS Web AppのURLは端末のlocalStorageにのみ保存されるため、他のユーザーが同じアプリを別ブラウザ・別端末で開いても（隠しトリガーを見つけたとしても）URL欄は空欄になり、開発者のスプレッドシートに書き込まれる心配はない
  - jsdomでdirty追跡・隠しトリガー・localStorage永続化・fetch送信内容（宛先URL・POST・Content-Type・payload構造）・CSV出力の同一性を検証（計26アサーション）
- [x] **タイマー音・バッジ・回数ドラム・重量刻み（2026-07-14 実装 / v0.7.0）**：4件のフィードバック対応
  1. **タイマー音がAmazon Audible等の再生中に鳴らない問題への対処**：iOS SafariはWeb Audio API（`AudioContext`でオシレーター合成）よりも`<audio>`要素のほうがバックグラウンド再生中の他アプリと共存しやすい傾向があるとされる（確実に直る保証はない対症療法。[Apple Developer Forums](https://developer.apple.com/forums/thread/48815)等を参照し方針をユーザーと合意）。既存の5音色（ビープ/ベル/チャイム/電子音/ソフト）の合成ロジック（`scheduleTone`等）はそのまま維持し、`OfflineAudioContext`でAudioBufferにレンダリング→自前のWAVエンコーダ（`audioBufferToWavBlob`）でBlob化→`<audio id="timerAlarmAudio">`要素で再生する方式に変更。iOSはユーザー操作から非同期処理を挟んだ後の`play()`を許可しないことが多いため、`unlockAudio()`内で無音の短いWAV（`SILENT_WAV_URL`、同期的に生成しdata URI化）を**同期的に**一度再生→即停止してアンロックし、実アラーム音は別途非同期でレンダリングして`alarmRenderCache`にキャッシュ（タイマー開始時に事前レンダリングしておき、終了時の`playAlarmSound()`は待ち時間なく再生）。`OfflineAudioContext`非対応環境では従来のAudioContext直接再生にフォールバック。Chromeヘッドレスで`<audio>`要素のsrc切り替わり・`currentTime`進行を確認済みだが、実際にiOS実機でAudible再生中でも鳴るかは未検証（次回iPhone実機確認が必要）
  2. **タイマー終了後アプリを開いてもバッジが消えない不具合を修正**：原因は`visibilitychange`ハンドラが「タイマー実行中の場合のみ」経過時間を再計算する作りで、TIME UP画面のままアプリを閉じて再度開いてもバッジをクリアする条件に入らなかったこと。アプリが表示状態になった時点で、タイマーの状態によらず無条件で`clearBadge()`を呼ぶよう修正
  3. **回数入力にドラムロールピッカーを追加**：重量ドラムと同じ操作方式（上下スクロール＋スナップ、マウスドラッグ対応、直接入力欄併存）で`#repsSheet`を新設。範囲は0〜50回・1刻み。回数セルは数字タップでドラムが開くボタン式に変更（`w-display`クラスを流用し重量セルと統一感のある見た目に）。±ボタンは従来どおり1刻みで併存
  4. **重量ドラムロールの刻みを0.25kgに変更し、±ボタンの刻み幅を設定で選べるように**：ドラムの目盛りを0.5kg刻み→0.25kg刻みに変更（0.5kg・1.25kg・2.5kgなど主要なプレート単位すべてにピッタリ止まれる）。一方「1.25kg・2.5kgずつサクッと増減する」という操作性はドラムの細かい刻みだけでは実現できないため、±ボタン側に「重量ステップ」設定を追加（設定タブ「記録」セクション、0.5kg/1.25kg/2.5kgから選択、`kintore_weight_step`にlocalStorage永続化）。±ボタンは選んだ刻み幅で増減する
  - Playwright（Chromeヘッドレス）でドラム項目数・直接入力からの選択確定・設定変更と±ボタンの連動・visibilitychange発火・タイマー終了時の`<audio>`要素再生（src切り替え・currentTime進行）を確認。コンソール/ページエラーなし
- [x] **CSVインポート（2026-07-17 実装 / v0.7.0）**：エクスポートしたCSVからの復元。`parseCSV`（引用符・改行・""エスケープ対応）→`buildImportData`（ヘッダー名で列判定・日付/種目ごとにグルーピング）→`DB.applyImport`（対象日を丸ごと置き換え・種目マスタに無い種目は`resolveExercise`で自動登録）。取り込み前に全状態を`PREIMPORT_BACKUP_KEY`へ自動退避し「直前の取り込み前に戻す」で復旧可能
- [x] **入力・引き継ぎの不具合修正など4件（2026-07-21 実装 / v0.7.1〜v0.7.4）**
  - v0.7.1: 種目を削除→同名で再登録するとIDが変わり、前回重量の引き継ぎと「前回」表示が効かなくなる問題を修正（`prevRecord`がID不一致時に記録時スナップショット「名前+部位+器具」の完全一致でも同一種目とみなすフォールバックを追加。器具違い・部位違いの同名種目は従来どおり別種目扱い）
  - v0.7.2: 重量・回数ドラムの「数字で直接入力」欄をプリフィルせず常に空欄に（「1」が入った状態で5を打つと15になる誤入力を防止。開いた時もホイール操作時も入力欄には書き込まない。確定値はホイール選択が持つため保存動作は不変）
  - v0.7.3: 重量±ボタンの刻み幅に1kgを追加（0.5/1/1.25/2.5/5から選択）
  - v0.7.4: ピッカー入力後に画面が下の種目へ飛ぶ問題を修正（ホイール端でのスクロール連鎖・直接入力のキーボード表示で背後のページがずれるのが原因。開いた時点のページ位置を記憶し閉じる時に復元＋250ms後にもう一度復元＋`.drum-scroll`に`overscroll-behavior: contain`。重量・回数・有酸素時間の3ピッカーすべてに適用）
- [x] **Phase 5：自動バックアップ＆復元（2026-07-22 実装 / v0.8.0）**
  - **背景**：iPhoneではSafariとホーム画面PWAでlocalStorageが完全に分離されており、開き方が変わると記録が「消えた」ように見える。さらにSafariタブ側は7日間未使用でiOSがサイトデータを自動削除することがある。スプレッドシートをマスターデータにして、どの環境からでも記録を取り戻せるようにする（Phase 4の「手動・一方向のみ」の決定をここで更新）
  - **自動バックアップ**：アプリ起動時＋画面がバックグラウンドへ移った時（`visibilitychange`=hidden、`keepalive`付きfetch）に、未送信差分（`dirtyDates`）があれば自動送信。GAS URL未設定・オフライン・失敗時は静かにスキップ（dirtyが残り次の機会に自動再送）。手動「今すぐバックアップ」ボタンも残す
  - **種目マスタのバックアップ**：毎回のバックアップに全種目リスト（部位・種目・器具・動画URL・メモ）を同梱し、スプシの「種目」シートを丸ごと書き換え保存。登録だけして未使用の種目も守られる
  - **復元**：クラウド同期セクションに「スプレッドシートから復元」ボタンを追加。GASに`action:'restore'`をPOSTすると全記録行＋種目リストがJSONで返り、CSVインポートと同じ`buildImportData`→`DB.applyImport`で取り込む（`ROW_HEAD`を先頭に足して流用）。種目は`DB.importExercises`で不足分のみ追加（video/noteはローカルが空の場合のみ設定）。復元前に`PREIMPORT_BACKUP_KEY`へ自動退避（「直前の取り込み前に戻す」で復旧可能）。復元した日付は`clearDirty`（スプシと一致しているため再送不要）
  - **起動時の復元導線**：記録が1件もない場合、記録タブの空状態に「クラウドバックアップから復元」ボタンを表示。GAS URL未保存ならprompt入力→復元（隠し機能の解除状態と独立して使える）
  - **gas/code.gs v2**：`doPost`に`action`分岐を追加（省略時は従来の書き込みで後方互換）。restore時は記録シート全行（日付は`yyyy-MM-dd`文字列に正規化）＋種目シートを返す。**ユーザー作業**：既存スプシのApps Scriptにコードを貼り直し「新バージョン」で再デプロイ（URLは変わらない）
  - **運用ルール**：普段はホーム画面アプリだけで使う（保存場所を1つに固定）ことを推奨
- [x] **Phase 5 続き：同期の不具合修正・運用改善（2026-07-23〜25 実装 / v0.8.1〜v0.8.4）**
  - v0.8.1: 種目追加時の前回引き継ぎを上から3セットまでに制限（4セット目以降は「＋セット追加」で。有酸素は全セッション引き継ぎのまま）
  - v0.8.2: 誤ったURL宛の送信でJSONでない応答を`{ok:true}`扱いにして「送信済み」と誤記録するバグを修正（res.okチェック＋JSONパース失敗を失敗扱いに）。手動バックアップ時に未送信が無ければ「全記録を送り直しますか？」を提案（回復手段）。URL欄にscript.google.com以外を入力したら警告
  - v0.8.3: 種目マスタの変更（追加/名称変更/情報更新/削除/並べ替え）を`state.dirtyExercises`で未送信変更として追跡。記録の変更が無くても種目だけ送信できる。既存データはフィールド追加時にdirty扱いで初回同期に確実に乗せる
  - v0.8.4: バックアップ済み時のconfirm文言を「✅ すべてバックアップ済みです」に改善。JS/CSSの読み込みURLに`?v=`を付与（GitHub PagesのHTTPキャッシュmax-age=600でindex.htmlとJSの新旧が混在するのを防止。**バージョンアップのたびに`?v=`も更新すること**）。sw.jsのオフラインフォールバックを`ignoreSearch`でクエリ非依存に
- [x] **Phase 6：一般公開準備・ご意見受付（2026-07-25 実装 / v0.9.0）**
  - **背景**：一般公開にあたり、(1)一般ユーザーに見せるべきでない開発者向けUIの整理、(2)ユーザーからの不満・改善要望を受け取る窓口が必要
  - **ご意見・ご要望フォーム**：設定画面に「サポート」セクションを追加。本文（最大1000文字）＋メールアドレス（任意・空欄OK。「内容についてこちらからご連絡することがあります。」の注記）＋送信ボタン。送信先は受付専用GAS（`gas/feedback.gs`・新規）で、受付スプレッドシートの「ご意見」シートに日時・本文・メール・アプリバージョンを蓄積し、オーナー宛にメール通知（MailApp）。スパム対策：本文必須・1000文字制限・同一端末から1日5回まで（`kintore_fb_日付`でカウント）
  - **受付GAS URLの扱い**：公開前提の受付窓口のため`FEEDBACK_GAS_URL`定数としてコードに直接埋め込む（バックアップ用GASの「埋め込まない」方針とは別物：書き込み専用で記録データとは無関係）。**空文字の間はサポートセクション自体が非表示**（ユーザーのGASデプロイ後にURLを埋めて有効化する）
  - **一般公開向け整理**：記録0件時の「クラウドバックアップから復元」ボタンを、クラウド同期を使っている端末（隠し機能解除済みまたはGAS URL設定済み）だけに表示。設定画面に「プライバシー」セクション（データは端末内保存・開発者は記録を見られない旨）を追加
  - 通知メール宛先は`NOTIFY_EMAIL`定数（空ならスプレッドシートのオーナー宛）
- [x] **Phase 7-0：公開URL・コミット履歴の匿名化（2026-07-26 実施）**
  - **背景**：App Store公開を見据え、公開物から開発者の実名を外したい。実名が露出していた箇所は (1)GitHub PagesのURL（`chihirohonma.github.io`）、(2)リポジトリのオーナー名、(3)全40コミットの作者名とメールアドレス の3つ
  - **実施内容**：`git filter-repo --mailmap` で全コミットの author/committer を `kanaeru-apps <kanaeru-apps@users.noreply.github.com>` に書き換え、組織アカウント `kanaeru-apps` に**新規リポジトリを作って通常pushした**（既存リポジトリへのforce pushではない）
  - **なぜ移管(transfer)ではなく新規作成か**：force pushだと書き換え前の古いコミットがGitHub側に到達可能なオブジェクトとして残り、コミットSHAを直接指定すれば実名・メールが読めてしまう。新規リポジトリなら書き換え後の履歴しか存在せず、旧URLからのリダイレクト（＝個人アカウントとの紐付け）も発生しない。star/fork/issueはいずれも0件で失うものが無かった
  - **ローカル設定**：このリポジトリ限定で `git config user.name/user.email` を `kanaeru-apps` に設定済み（他プロジェクトのコミットには影響しない）。今後このリポジトリでコミットする際に実名が再混入することはない
  - **旧リポジトリ**：`ChihiroHonma/kintore-log` は移行確認後に非公開化する（公開したままだと実名入りの履歴が残るため）
  - **PWAへの影響**：オリジンが変わるためlocalStorageは引き継がれない。`manifest.json`は`start_url`/`scope`とも相対パス（`./`）のため変更不要。移行手順は「新URLをホーム画面に追加 → 設定でGAS URLを入力 → スプレッドシートから復元」
- [x] **v0.9.2：GAS URL取り違えの再発防止（2026-07-27 実装）**
  - **事象**：バックアップ先URLの欄に、スプレッドシートの閲覧URL（`https://docs.google.com/spreadsheets/d/…/edit?gid=…`）を貼ってしまいバックアップできない、という取り違えが2回発生した
  - **なぜ起きるか（根本原因）**：(1)機能名が「スプレッドシートにバックアップ／から復元」なので、手元にあるスプレッドシートのURLを貼るのが利用者にとって自然に見える (2)v0.8.2で設定画面のURL欄には警告を足したが、**警告トーストを出すだけで誤ったURLをそのまま保存していた** (3)後から追加した復元promptには検証を横展開しておらず素通りだった (4)URLの妥当性判定が入力箇所ごとに散らばっていて共通化されていなかった
  - **対策**：判定を`checkGasUrl(input)`1か所に集約し、設定画面・復元promptの両方が必ずここを通る形にした。**不正なURLは保存しない**（設定画面では保存済みの正しいURLを維持し入力欄も元に戻す）。エラーは原因別に具体的な文面を出し分ける：`docs.google.com`を含む場合は「それはスプレッドシートを開くためのURLです。必要なのは…/exec です」、`https://script.google.com/`で始まらない場合、末尾が`/exec`でない場合（`/dev`は開発用で使えない旨）。空欄はクラウド同期の解除として許可
  - Playwrightで12項目を検証（誤URLでalertが出る／保存されない／正しいURLは通る／設定欄で既存URLが壊れない／空欄で解除できる／JSエラーなし）
- [x] **v0.9.3：種目バックアップをマージ＋削除同期に変更（2026-08-02 実装）**
  - **背景（発見のきっかけ）**：「アプリ側の種目が10個・スプレッドシート側が20個の状態でアプリを開いたら、種目は10個に上書きされるのか？」という確認から判明した設計上の危険。答えは**上書きされる**だった
  - **何が起きていたか**：バックアップのたびにアプリ側の全種目を送り、GAS側は「種目」シートの2行目以降を`clearContent()`してから丸ごと書き直していた（旧`writeExercises_`）。そのため、種目が少ない端末が一度でもバックアップすると、シート側の種目がその端末の内容に潰れる。しかも自動バックアップは起動2秒後と画面切替時に走るため、**記録を1セット入力しただけでも巻き込まれる**（種目の変更が無くても種目リストは毎回同梱されるため）
  - **なぜ全置換だったのか（根本原因）**：Phase 5でこの機能を作った時点では「アプリが唯一のマスター、シートはその写し」という前提で設計していた。実際には (1)機種変更・PWA/Safariの保存領域分離で「種目が欠けた端末」が容易に生まれる (2)シートに直接手入力もできる ため、前提が成立していなかった
  - **対策**：シートへの反映をマージ方式に変更し、削除は明示的に伝える方式にした
    - アプリ側（`db.js`）：削除した種目を`state.deletedExercises`（tombstone）に控える。`deleteExercise`で積み、`renameExercise`では突合キーが変わるため**旧名を積む**（積まないとシートに旧名が残り、復元で重複して戻る）。同じ種目の再登録・復元時は控えを取り消す。控えが残っている限り`exercisesDirty()`はtrueのままで送りそびれない
    - GAS側（`code.gs` v3）：`writeExercises_(exercises, deleted)`をマージ処理に置換。アプリにある種目は追加/更新、**シートにしかない種目は残す**、`deleted`に含まれるものだけ消す。動画URL・メモはアプリ側が空ならシート側の値を維持（スプレッドシートに直接書いた内容を守る）。並び順はアプリ側優先、シート専用種目は末尾。アプリ側に存在する種目は削除指定より存在を優先（削除→同名で再登録の取り違え防止）
  - **突合キー**：種目シートにID列が無く、IDは端末ごとに採番されるため`部位+種目名+器具`の3点（`findExercise`と同じ基準）。区切り文字が種目名に混ざらないようJSON配列文字列をキーにしている
  - **既知の制約**：複数端末での削除は完全同期できない。端末Aで削除→シートから消えても、端末Bにその種目が残っていれば端末Bのバックアップで復活する（端末Bでも削除するか、端末Bで復元すれば解消）
  - **検証**：GASマージ単体テスト10項目（Nodeでシートをモック）、`db.js`のtombstone単体テスト10項目、Playwrightでの実機テスト6項目（削除→送信→クリアの一連、名称変更、JSエラーなし）、実ペイロードをGASに流す統合テスト（アプリ30種目 vs シート41種目で削除1件だけが消える／3種目しか持たない端末が同じシートに送っても減らない）をすべて通過
  - **ユーザー作業**：`gas/code.gs`をスプレッドシートのApps Scriptに貼り直し、「デプロイを管理」→鉛筆→「新バージョン」で再デプロイ（URLは変わらない）。**アプリだけ更新してGASが旧版のままだと全置換の挙動が残る** → **2026-08-07 実施済みを確認**（判定方法：Web Appに`{"dates":[],"rows":[]}`をPOSTし、応答に`exercises`/`deleted`が含まれればv3。v2の応答は`{ok,dates,rows}`のみ。この空POSTは記録の増減なし・種目シート非干渉で安全）
- [x] **v0.10.0：有酸素の入力改善＋インターバル記録（2026-08-09 実装）**
  - **背景**：(1)距離などの数値欄をタップすると指の位置にキャレットが立ち、既に入っている値を消しにくい (2)エアロバイクのHIIT（例：全力30秒／流し90秒 × 8本）を記録する手段が無く、1セッション＝1行の作りでは何本目がどちらか判別できなかった
  - **決定事項（ユーザー合意済み）**：入力は「末尾キャレット＋✕クリアボタン」、記録方式は「強度タグ＋まとめて本数追加」、強度ラベルは **WORK / REST**（4文字ずつでチップ幅がぶれない・見た目重視で選定）
  - **キャレット末尾寄せ**：`focusin`だけでは不十分。iOS Safariは`focus`の**後**にタップ位置へキャレットを置き直すため、`focusin`＋`pointerup`（`setTimeout(0)`で後追い）の二段構えにした。`selectionStart !== selectionEnd`（ドラッグでの範囲選択）のときは何もしない＝一部だけ選んで打ち替える操作は従来どおり使える
  - **✕クリアボタン**：値がある欄にだけ表示（`.cf-clear.on`）。`click`ではなく`pointerdown`＋`preventDefault()`で処理する。clickだとボタンにフォーカスが移ってソフトキーボードが一度閉じてしまい、続けて打ち直せないため
  - **強度タグ**：セットに`z`（`'hi'`=WORK / `'rec'`=REST / `''`=なし）を追加。チップのタップで WORK→REST→なし と巡回。**タグが付いたセットがある種目は、1セッション＝1枚のカード表示から、1行＝1本のコンパクトなラップ行（`.lap`）表示に切り替わる**（行をタップすると距離・速度などの入力欄が開く）。タグが1つも無ければ従来のカード表示のまま＝既存ユーザーの見た目は変わらない
  - **⚡インターバル一括生成**：WORK秒／REST秒／本数を指定して一気に生成するシート（`#genSheet`、既存の有酸素時間ピッカーと同じドラム見た目）。設定は端末に記憶（`kintore_interval_cfg`）。値が何も入っていないセッションしか無ければ**置き換え**、既に入力済みなら**末尾に追加**（プレビュー欄に合計時間・セッション数・どちらのモードかを表示）。REST 0秒ならWORK行だけを作る
  - **秒の集計漏れを修正**：インターバルは30秒・90秒がすべて秒(`ts`)側に入るため、`+s.t`（分）だけを読んでいた3か所（`dayStats`の合計時間・前回/履歴の要約・`charts.js`の有酸素グラフ）が0分になっていた。`setSeconds(s)`に集約して修正
  - **表示の時間書式**：`fmtSeconds`は0分のとき分を省く（「0分30秒」ではなく「30秒」）。要約用の`fmtShortSec`はちょうどの分・時間で末尾の`00秒`を出さない
  - **連続入力が1つ飛ぶ不具合を修正（本改修で発見）**：速度→心拍→距離と続けてタップ入力すると心拍だけが保存されない事象。なぜなぜ分析の結果、根本原因は**「変更したら`renderLog()`で全再描画」という単一の更新経路**にあった。筋トレのセットはドラム・±ボタン入力でフォーカスを保持しないため全再描画で害が無く、後から足した有酸素のテキスト入力がその経路をそのまま流用していた。次の欄に指が触れた瞬間に前の欄のblur→再描画が走り、タップ先の要素が消えて入力が失われる。対策として`renderDayStats()`を`renderLog`から切り出し、`change`ハンドラは全再描画をやめて部分更新（`refreshCardio`：値の正規化・クリアボタン・合計・強度別内訳・その行の要約・サマリータイル）に変更した
  - **CSV**：18列目に`強度`を**末尾に**追加（途中に挿すと、スプレッドシートに既に書かれている行と列がずれるため）。値は`WORK`/`REST`/空。取り込みは`WORK/強/HI`・`REST/緩/REC`のいずれも受け付け、列が無い17列の旧CSVもそのまま読める
  - **gas/code.gs v4**：列数を`HEADER.length`固定ではなく`colCount_(sheet)`（実際の使用列数と定義の大きい方）で扱うようにし、既存シートのヘッダー空欄を自動で埋める。書き込み時は行の最大幅に合わせてパディング。→ **17列でも18列でも受け取れる**
  - **ユーザー作業（順序に注意）**：**先に`gas/code.gs`（v4）を再デプロイしてから**アプリv0.10.0を配布する。逆順だと18列の行を17列の範囲に書こうとして同期がエラーになる
  - **検証**：Playwrightで42項目（キャレット末尾・ドラッグ範囲選択の維持・クリアボタンとフォーカス・強度チップの巡回・一括生成と設定記憶・置換/追加モード・CSV 18列の出入り・17列CSVの後方互換・連続入力の保存）＋`db.js`のNode単体テスト＋GASのマージ処理を確認
- [x] **v0.10.1：インターバル生成を「分＋秒」入力に（2026-08-09 実装）**
  - **背景**：v0.10.0の一括生成シートはWORK/RESTが秒だけの1欄だったため、3分×5本のような長いインターバルを「180」と秒換算で打つ必要があり分かりにくかった
  - **実装**：WORK/RESTを分欄・秒欄の2つに分割（`#genWorkM`/`#genWorkS`、`#genRestM`/`#genRestS`）。**内部は従来どおり「合計秒」で持ち**、画面に出すときだけ分・秒へ割る（`showGen`）／読むときだけ合計秒へ戻す（`readGenTime`）。保存形式・`doGen`・CSVは一切変えていない。±ボタンも合計秒に5秒足すだけなので、`2分00秒`で−5秒を押すと`1分55秒`と自然に繰り下がる
  - **入力中は値を直さない**：`input`ではプレビューだけ更新し、範囲への丸めと分・秒への割り直しは`change`（確定）で行う。こうしないと「3」と打つ途中で勝手に丸められたり、分を消している最中に0が書き戻されたりする。秒欄に`90`と打っても弾かず、確定時に`1分30秒`へ繰り上がる
  - **`.stepper`のスコープ漏れを修正（v0.10.0の回帰）**：v0.10.0で生成シート用に**素の`.stepper`**を追加してしまい、同じ詳細度の後勝ちで**記録カードの重量・回数ステッパーまで巻き込んでいた**（角丸12→10px、`align-items` stretch→center、ボタン36px/19px→34px/17px+高さ40px固定）。`#genSheet .stepper`配下に閉じ込めて修正。**生成シート用のCSSは必ず`#genSheet`で始めること**（CSSにも警告コメントを残した）
  - **数値欄のキャレットは「欄の編集のされ方」で分ける（なぜなぜ分析の結論）**：v0.10.1の実装当初、記録カード用の末尾寄せを`bindCaretToEnd`として一般化し生成シートにも一律適用したところ、**分・秒欄が打ち直せなくなった**（フォーカス時に選択範囲が末尾へ畳まれるため、全選択→上書きが「追記」になり`30`が`3015`になる）。根本原因は、欄を「追記編集する欄／全部打ち直す欄」という**編集の性質**で分類せず、"数値欄"という見た目の共通点だけでふるまいを共有したこと。対策として`bindCaretPlacement(root, sel, place)`に一般化し、**有酸素の距離・時間・心拍（桁数が多く✕クリアボタンもある）＝末尾寄せ**、**生成シートの分・秒・本数（1〜2桁で必ず丸ごと打ち直す）＝タップで全選択**と使い分けた
  - **検証**：Playwright 51項目すべて通過（分＋秒の合計反映・秒90の繰り上がり・分をまたぐ−5秒・上下限クランプ・全選択して上書きできること・記録カード側は末尾寄せのまま・ドラッグ範囲選択の維持）。記録カードのステッパー実寸がv0.10.0以前と一致することも数値で確認（角丸12px／高さ44px／ボタン36px・19px）
  - **GASの変更は不要**：CSVの形式は変わっていないため、v4のままでよい
- [x] **v0.11.0：ワークアウトデイを履歴タブの先頭へ移設＋日付/部位で履歴を絞り込む（2026-08-10 実装）**
  - **背景**：カレンダーはグラフタブの先頭にあり「実施日を眺めるだけ」の存在だった。過去の特定日を見たいときは履歴を延々スクロールするしかなく、カレンダーと履歴が別タブに分かれていること自体が遠回りだった
  - **決定事項（ユーザー合意済み）**：(1)履歴タブへ**移動**（グラフタブからは消す） (2)部位チップも一緒に持ってきて**履歴一覧も同じ部位で絞る** (3)カード全体を**折りたためる**（既定は開いた状態、開閉だけ端末に記憶）
  - **絞り込みの仕様**：日をタップ＝その日だけを一覧に出す（中身も開いた状態で表示）／同じ日を再タップで解除／部位チップはカレンダーの塗り色と一覧の両方に効く／何も選んでいなければ**従来どおりの全件表示**。一覧の上に「2026/8/6(木) の記録」＋「すべて表示」のバーを出す（月を送って選択日が画面外に出ても、何で絞っているか分かるようにするため）
  - **`has`（一覧に出る日）と`trained`（塗る日）を分けた理由**：種目を足しただけで数値が未入力の日も履歴には出る。塗られた日だけ押せる作りにすると「一覧に並んでいるのに選べない日」ができて理由が分からない。そこで`histDayInfo()`が2つのマップを返し、**hasの日はすべて`<button>`＝押せる**（未入力の日は塗らずに下の小さなドットで示す）、**trainedの日だけ塗る**、記録の無い日と未来日は`<span>`のままにした
  - **選択日が宙に浮かないようにする**：部位を変えてその日が対象外になったとき／選択中の日を削除したときは、選択を自動で外す。残したままだと「該当する記録がありません」だけが出て、なぜ0件なのか分からなくなる
  - **選択日は端末に記憶しない**：記憶するのは開閉状態（`kintore_hist_cal_open`）だけ。アプリを開き直したときは全件から始めたいため
  - **部位カラーを`DB.PART_COLOR`へ集約**：カレンダーの塗り（app.js）とグラフの線色（charts.js）で同じ配色を使うため、部位の情報を持つ`db.js`に置いた。両ファイルにコピーすると色を変えたとき片方だけ直す事故になる
  - **CSSは必ず`#calGrid`でスコープする**：`.cal-day`は**記録タブの日付ピッカー（`#dateCalGrid`）と共用**しているため、無スコープで書くとそちらを巻き込む（v0.10.1で`.stepper`を無スコープに書いて実際に別画面を壊している）。選択リングも記録タブ側の`.selected`とは別名の`.sel`にした。リングは`box-shadow`を2段にしてパネル色の隙間を挟む（部位ALLだと塗りもボルトイエローなので、隙間が無いとリングが塗りに溶けて消える）
  - **検証**：Playwrightで44項目（履歴タブに出てグラフタブから消えている／カレンダーが一覧より上／記録の無い日は押せない／未入力の日はドット表示で押せる／日を選ぶと1件に絞られ中身も開く／再タップで解除／部位で一覧と塗り色が変わる／対象外になった選択日が自動で外れる／すべて表示で戻る／月送りしても選択は保持／折りたたみが再読込後も残る／選択中の日を削除しても0件表示にならない／**記録タブの日付ピッカーが無改変**／JSエラーなし）＋既存の51項目とNode単体テストも通過
  - **GASの変更は不要**：データ形式・CSVとも変更なし
- [x] **v0.11.1：絞り込みバーを「すべて表示」だけにして中央寄せ（2026-08-10 実装）**
  - **背景（ユーザー指摘）**：v0.11.0のバーは「2026/8/9(日) の記録」＋「すべて表示」を左右に並べていたが、**日付のすぐ横に「すべて表示」があるせいで「その日の全部を表示するボタン」と読めてしまう**。実際に押すと他の日も含む全件に戻るため、表示と挙動が食い違って見えた
  - **なぜ日付を書いていたか／なぜ不要だったか**：「月を送って選択日が画面外に出ても分かるように」という意図だったが、選択日はカレンダーでリング表示され、下のカードにも日付が出ている。**同じ情報を三度出したうえに、隣のボタンの意味まで誤読させていた**
  - **対策**：バーは「すべて表示」ボタン1つだけにして中央寄せ。絞り込み中（日付選択中 または 部位がALL以外）のときだけ出す点は変更なし。`.hfb-*` のCSSは削除
  - **検証**：Playwright 45項目（バーの中身が「すべて表示」だけ／ボタンが中央にある／部位だけの絞り込みでもバーが出る／月送りしてもバーが残る）＋既存51項目も通過

---

- [ ] **Phase 7：App Store公開（要件定義 2026-08-10・実装未着手）**

### 7-1. 方針と全体像

PWA版（`kanaeru-apps.github.io/kintore-log/`）は**今のまま残し、自分専用ツールとして使い続ける**。App Store版はそこから分岐した「一般ユーザー向けの別ライン」として作る。両者はデータを共有しない。

| | PWA版（現行・自分用） | App Store版 v1.0 | App Store版 v1.1 |
|---|---|---|---|
| 配布 | GitHub Pages | App Store | App Store |
| データ保存 | localStorage | localStorage＋Documents書き出し | 同左 |
| クラウド同期 | GAS Web App（URL手入力） | **なし** | Googleログイン＋Sheets API |
| タイマー通知 | 画面ロック中は鳴らない | **ローカル通知で鳴る** | 同左 |

**v1.0で同期を載せない理由**（2026-08-10 ユーザー決定）：GAS方式は「自分でGASプロジェクトを作ってデプロイしてURLを貼る」という手順が必要で、一般ユーザーには使えない。かといって「上級者向け」として畳んで載せると、App Store説明文・Review Notes・アプリ内警告文の3か所に説明を書く必要が出る（ガイドライン2.3.1が hidden / undocumented features を禁じているため）。**その説明コストはv1.1のGoogleログイン導入で全部消える**ので、暫定的に説明を書くより、v1.0では同期そのものを外して確実に審査を通し、公開を最優先する。

### 7-2. v1.0 のスコープ

**入れるもの**
- 現行アプリの全機能（記録・履歴・グラフ・タイマー・計算機・設定）
- **iCloudバックアップ対応**（7-3）
- **ローカル通知**（7-4）
- ご意見・ご要望フォーム（現行のまま。ユーザーサポート窓口として残す）
- CSVエクスポート／インポート（既存。手動バックアップ手段として重要）

**外すもの**
- **クラウド同期セクション一式**（GAS URL入力欄・自動送信・復元）

**決定済み事項**

| 項目 | 決定 | 決定日 |
|---|---|---|
| Bundle ID | `com.kanaeru.kintore` | 2026-08-09 |
| アプリ名 | **筋トレLog**（選定の経緯と競合調査は 7-11） | 2026-08-11 |
| 4.2対策 | ローカル通知（**タイマーのバックグラウンド通知のみ**。休養日リマインダーは不採用＝7-4） | 2026-08-09 / 2026-08-10修正 |
| 収益化 | 無料。将来的に広告を検討（v1.0では広告SDKを入れない＝App Privacy申告が単純になる） | 2026-08-09 |
| v1.0の同期 | なし | 2026-08-10 |
| 販売者名 | 個人名義のまま（本名表示を許容と決定済み・[[project-anonymization]]） | 2026-08-04 |
| ビルド | Capacitor＋Codemagic（`ツール_無音カメラ` の構成を流用） | 2026-08-09 |

### 7-3. データ保存とiCloudバックアップ

**課題**：現行アプリのデータは localStorage にある。Capacitor（WKWebView）の localStorage が iOS の端末バックアップに含まれるかは**未検証**（`Library/WebKit/` 配下に置かれるが、iOSがWebKitのデータをバックアップ対象として扱うかはバージョン差がある可能性がある）。ここを推測で「大丈夫」と扱うと、機種変更で記録が消える事故になる。

**対策（確実側に倒す）**：localStorage を正本のまま残しつつ、**`Documents/` にJSONを二重書き出しする**。`Documents/` は iOS のバックアップ対象であることが仕様として明示されている領域。

- 書き出し：`@capacitor/filesystem` で `Documents/kintore-backup.json` へ。タイミングは現行のGAS自動送信と同じ**起動時＋`visibilitychange(hidden)`**（＝既存の同期フックをそのまま流用でき、実装量が小さい）
- 読み戻し：起動時に記録が0件で、かつ `Documents/kintore-backup.json` が存在すれば、そこから復元する（機種変更・アプリ再インストール時の自動復旧）
- データ量：実測で1ヶ月20.5KB＝**年間約240KB**。10年でも2.4MBなので容量の懸念はない

**空データでバックアップを潰さないための順序制御（実装時に追加）**：機種変更直後は記録が空の状態で起動するため、読み戻しが終わる前に書き出しが走ると、**空のデータでバックアップを上書きして復元元を自分で消す**。`nativeRestorePending` フラグを立て、読み込み中は書き出しを行わない。また読み戻しは「記録が0件のときだけ」実行し、記録がある端末では絶対にファイルへ触れない。

**設定画面での見せ方**（一般ユーザーは「見えない機能」を信用しないので、動いている証拠を出す）:
```
 データの保存
 ┌────────────────────────────────────────┐
 │ ✓ 記録はこのiPhoneの中に保存されます        │
 │ ✓ iPhoneのバックアップに自動で含まれます     │
 │   最終保存：2026/08/10 10:07               │
 │                                          │
 │ 機種変更や紛失のときは、iPhoneのバックアップ  │
 │ から復元すれば記録もそのまま戻ります。       │
 │ 設定は必要ありません。                      │
 ├────────────────────────────────────────┤
 │ [ バックアップファイルを書き出す ]  ← 既存CSV │
 └────────────────────────────────────────┘
```

### 7-4. ローカル通知（4.2対策）

**なぜこれが4.2対策になるか**：現行PWAには「**画面ロック中はタイマーの通知音が鳴らない**」という、iOS WebView固有の制約がある（本書「実装ノート・制約」に記載）。ネイティブ化＋ローカル通知はこれを解消する。つまり「Webの見た目をラップしただけ」ではなく、**ネイティブでなければ実現できない価値がある**ことを審査官に説明できる。

**実装内容**：**インターバル／レストタイマーのバックグラウンド通知のみ**。タイマー開始時に完了時刻へローカル通知をスケジュールし、アプリを閉じても画面ロック中でも鳴らす。タイマーを止めたら通知をキャンセルする。

**必要な追加**：`@capacitor/local-notifications`、`Info.plist` の通知許可、初回タイマー使用時の許可ダイアログ（起動直後に出すと拒否されやすいので、**タイマーを初めて使うタイミング**で出す）

**実装の詳細（2026-08-10 実装）**：通知IDは `1` 固定（同時に走るタイマーは1本のため、常に上書き＝取り違えが起きない）。予約と取り消しの対応は次のとおり。

| きっかけ | 動作 |
|---|---|
| タイマー開始／再開 | 終了時刻に予約（`allowWhileIdle: true`） |
| ＋30秒／−30秒で終了時刻が動く | いったん取り消して予約し直す |
| 一時停止 | 取り消す（止めている間に鳴らさない） |
| リセット | 取り消す |
| アプリが開いたまま終了時刻に達した | 取り消す（画面側で音・バイブが鳴るため二重に鳴らさない） |
| 設定で通知をOFF | 取り消す。ONに戻したとき計測中なら予約し直す |

権限は `ensureNotifPermission()` で、**未決定のときだけ1回**要求する（拒否済みなら二度と出さない）。

**「休養日リマインダー」は入れない（2026-08-10 ユーザー決定）**：当初は4.2対策を厚くする目的で「最終トレーニング日からN日経過で通知」を併記していたが、(1)**4.2の説明はタイマー通知だけで成立する**（既存の実害を潰す機能であり、取ってつけた通知ではない）、(2)通知機能が増えるほど初回の権限要求が重くなり一般ユーザーに嫌われる、(3)そもそも「休養日リマインダー」という名前が**「今日は休養日です」と読めるのに実際はサボり検知**で、名前と中身が食い違っていた（v0.11.1の「すべて表示」ボタンと同じ誤読の構造）。必要になれば後から追加できる。

### 7-5. GAS同期の除外方法

コードを残したまま画面から隠すのは、ガイドライン2.3.1（hidden features）の観点で望ましくない。**ビルド時にソースごと除去する**方式にする。

`build-ios.js`（新規）がリポジトリ直下のソースを `www/` へコピーしながら変換する。**PWA版のソースは一切変えない**（マーカーコメントを足すだけ）ので、自分の運用に影響しない。

**方式：マーカー＋スタブ差し替え（2026-08-10、実装時に変更）**

当初は「マーカーで囲んだ範囲を削除する」つもりだったが、実装前にソースを調べたところ、同期関数はアプリの各所から**7か所**で呼ばれていた（記録タブの空状態・設定画面の描画・イベント登録・起動時・画面切替時）。削除するだけでは呼び出し側が `ReferenceError` になり、**アプリ自体が起動しなくなる**。そこで次の方式に改めた。

- ソースを `/* @sync:start */` 〜 `/* @sync:end */` で囲む（現在**3ブロック**：同期関数の本体／記録タブからの復元導線／設定画面のイベント登録）
- ビルド時、**1つ目のブロックを「何もしない同名関数10個」のスタブに差し替え**、2つ目以降は削除する
- 呼び出し側のコードは**一行も書き換えない**。PWA版とApp Store版でソースを分岐させずに済む
- ブロック数が想定と変わったらビルドを中止する（`EXPECTED_BLOCKS`）。マーカーの書き損じに気づかないまま出力しないため

**自動検証（`build-ios.js` に内蔵）**：生成後の `www/js/app.js` と `www/index.html` から、同期を実際に動かす部品（`kintore_gas_url` / `kintore_last_sync` / `kintore_sync_unlocked` / `gasUrlInput` / `syncNowBtn` / `restoreCloudBtn` / `action: 'restore'`）が消えていることを確認し、残っていればビルドを中止する。

- 検査はスタブの範囲（`@sync:stub-start` 〜 `@sync:stub-end`）を除いて行う。スタブ自身が `runSync` などの名前を含むため、除かないと自分の生成物を誤検出する（実際に一度これで止まった）
- `script.google.com` は**ご意見・ご要望フォーム**（`FEEDBACK_GAS_URL`）で正規に使うため、文字列の存在自体は禁止しない
- ビルド環境の注意：`fs.cpSync(..., {recursive:true})` は Node v24.14.1 / Windows でプロセスごと異常終了する（終了コード 0xC0000409）。`build-ios.js` は自前の再帰コピーを使っている

### 7-6. 申請物一式

| 項目 | 状態 | 内容 |
|---|---|---|
| アプリ名 | ✅ 決定 | **筋トレLog**。サブタイトル・キーワードとあわせて 7-11。端末のホーム画面に出る名前（`CFBundleDisplayName`）は「筋トレ記録」で設定済み → **App名に合わせて要変更**（7-11） |
| アプリアイコン | ✅ 作成済み | `icons/icon-512.png` のダンベルをピクセル計測してSVGで組み直し、1024×1024で再生成（拡大ボケなし・アルファチャンネルなし）。`icons/icon-1024.png` に控えを保存 |
| 起動画面 | ✅ 作成済み | 同じダンベルを黒背景の中央に配置（2732×2732）。Capacitorのデフォルト画像を差し替え済み |
| プライバシーポリシー | ✅ 作成済み | `privacy-policy.html`。記録は端末内のみ／端末バックアップはApple管理下／ご意見フォームは本文・メール（任意）・バージョンの3点のみ／通知はローカル通知／第三者提供なし／トラッキングなし |
| サポートページ | ✅ 作成済み | `support.html`。問い合わせ先＋FAQ7項目。App Store Connect の「サポートURL」に指定する |
| App Privacy申告 | 未整理 | 「連絡先情報＞メールアドレス」を**アプリの機能**目的で収集・**トラッキングなし**・ユーザーIDに紐付けない（任意入力のため）。それ以外は収集なし |
| スクリーンショット | 未作成 | 6.9インチ・6.5インチ必須。記録／履歴カレンダー／グラフ／タイマーの4枚 |
| Review Notes | 未作成 | 下記 |

**公開URL**（GitHub Pages。リポジトリ直下に置いたのでそのまま配信される。**2026-08-10 に実際に開けることを確認済み**）
- プライバシーポリシー：`https://kanaeru-apps.github.io/kintore-log/privacy-policy.html`
- サポート：`https://kanaeru-apps.github.io/kintore-log/support.html`

**Review Notes 案（v1.0・英語）**
> This app is a workout log that works fully offline. No account, no login, and no server communication are required to use any feature.
> All records are stored locally on the device and included in the standard iOS device backup.
> The only network feature is an optional "Send feedback" form in Settings, where the user may voluntarily provide an email address to receive a reply. It is not required to use the app.
> Local notifications are used for the rest/interval timer so that it can alert the user while the screen is locked or the app is in the background.

### 7-7. v1.1（Googleログイン同期）の設計方針

v1.0公開後に着手する。**GASを完全に廃止し、アプリからGoogle Sheets APIを直接叩く**。

- **スコープは `drive.file` のみ**：Googleのスコープ分類で **Non-sensitive（Recommended）**＝追加のOAuth検証・年次審査が不要。Sheets API はこのスコープをサポートする
- **アクセス範囲**：「このアプリが作成したファイル」＋「ユーザーがピッカーで明示的に選んだファイル」のみ。ユーザーのDrive全体は見えない ← これがそのまま設定画面の説明文になる
- **GASが担っていた処理をアプリ側JSへ移植する**：種目のマージ＋tombstone削除（`writeExercises_`）、日付ソート（`sortByDate_`）、シートに直接書かれた動画URL・メモの保護（`pickValue_`）、同日データの置き換え（`deleteRowsForDates_`）
- **通信回数が増える**：GASは「POST 1回」で完結していたが、API直結は「読む→マージ→書く」の2〜3往復になる。複数端末同時更新時の競合リスクがわずかに上がる（現行GASにも排他ロックは無いため、質的な差は小さい）
- **既存のバックアップスプレッドシート（`1YAocqucmc4...`）の扱い**：`drive.file` では**アプリから見えない**（アプリが作成したファイルではないため）。データを引き継ぐ場合は「現行アプリで復元→端末にデータを載せる→Googleログイン版で新規シートへ書き出す」の手順を取る。既存シートは削除せずそのまま残す
- **未検証の関門**：iOSではWebView内でGoogleのログイン画面を直接開くことをGoogle側が禁止している（`disallowed_useragent`）。`ASWebAuthenticationSession` を使うCapacitorプラグイン経由での実装が必要。**ここは着手して初めて分かる詰まりがある可能性がある**ため、v1.0の公開を人質に取らない順序（＝v1.0公開後に着手）にしている
- 費用：Sheets API / Drive API とも無料枠内。OAuth審査も `drive.file` なら不要

**v1.1の設定画面 UI案**
```
 Googleスプレッドシートに保存
 ┌────────────────────────────────────────┐
 │        [  G  Googleでログイン  ]         │
 │ 記録をご自身のGoogleドライブに自動保存します。 │
 │ このアプリが作成したファイル以外には          │
 │ 一切アクセスしません。                     │
 └────────────────────────────────────────┘
 ── ログイン後 ──
 │ 保存先   筋トレ記録        [シートを開く]   │
 │ アカウント xxx@gmail.com   [ログアウト]     │
 │ 最終同期  2026/8/10 10:07                 │
 │ 変更は起動時と画面切替時に自動送信            │
 │ [今すぐバックアップ]  [スプレッドシートから復元] │
```
URL入力欄が消えて**「ログイン」1ボタン**になるため、「上級者向け」の折りたたみも警告文も不要になる。

### 7-8. 実装順序

1. ✅ Capacitorセットアップ（`package.json` / `capacitor.config.json` / `build-ios.js`）
2. ✅ GAS同期の除去マーカーを現行ソースに埋め込む（PWA版の挙動は無変更）
3. ✅ `Documents/` 二重書き出し＋起動時復元の実装
4. ✅ ローカル通知（タイマー）
5. ✅ 設定画面の「データの保存」セクション（7-3のUI）
6. ✅ 自動検証（Playwrightで PWA版の回帰と、Capacitorをモックしたネイティブ経路の両方）
7. ✅ プライバシーポリシー・サポートページを作成（`privacy-policy.html` / `support.html`）
8. ✅ `codemagic.yaml` を作成（無音カメラの構成を流用＋`node build-ios.js` を追加）
9. ✅ `npx cap add ios` でiOSプロジェクトを生成し、Info.plist・端末対応・アイコンを調整（7-9）
10. ✅ アプリ名を決定 → App Store Connect でアプリを作成（`筋トレLog` / Apple ID `6800222286`）
11. ✅ スクリーンショットを作成（6.9インチ・6.5インチ／7-13）
12. ✅ Codemagicにリポジトリを接続してビルド（2026-08-11・Index 4 で成功／7-14） → **TestFlightで実機検証が残**（**特にタイマーの画面ロック中通知**）
13. App Store Connect でメタデータ入力・App Privacy申告・審査提出

**2026-08-10 時点の検証結果**（実機前に確認できたこと）

| 確認項目 | 結果 |
|---|---|
| PWA版がPhase 7の変更で壊れていないか | ✅ コンソールエラーなし。種目追加・タイマー・設定画面すべて従来どおり。`データの保存`セクションはPWAでは非表示 |
| App Store版（`www/`）が同期スタブで起動するか | ✅ 起動する。コンソールエラーなし |
| 隠しコマンド（バージョン7回タップ）で同期が復活しないか | ✅ 何も出ない（2.3.1対策として重要） |
| タイマー開始→通知予約、停止→取り消し | ✅ `schedule(id=1, at=開始+60秒)` → `cancel(id=1)` を確認 |
| 画面を離れたときのバックアップ書き出し | ✅ `Documents/kintore-backup.json` へ1,876文字を書き出し |
| 機種変更の再現（localStorageを空にして再起動） | ✅ ファイルから自動復元され、種目・記録が戻る |

**実機でなければ確認できない残件**：ネイティブブリッジ経由で `Filesystem` / `LocalNotifications` が実際に応答すること、画面ロック中に通知音が鳴ること、`Documents/` がiCloudバックアップに含まれること。

### 7-9. iOSプロジェクトとCodemagicビルド設定

**iOSプロジェクトの生成場所を「Codemagic上」から「Windows上でコミット」に変更した**

当初は `npx cap add ios` をmacOS側（Codemagic）で毎回実施する想定だったが、それだと Info.plist の設定（表示名・向き・輸出コンプライアンス）やアイコンがビルドのたびに初期状態へ戻ってしまう。そこで **Windows で `cap add ios` を実行し、`ios/` をリポジトリにコミットする**方式にした（無音カメラと同じ）。

ただし Windows で生成すると1ファイルだけ壊れる：

```swift
// ios/App/CapApp-SPM/Package.swift（Windowsが生成したもの）
.package(name: "CapacitorFilesystem", path: "..\..\..\node_modules\@capacitor\filesystem")
```

パス区切りが `\` になり、macOSのビルドで解決できない。このファイルは冒頭に "DO NOT MODIFY THIS FILE - managed by Capacitor CLI" とある生成物で、**`cap sync` を実行するたびに必ず上書き生成される**（存在有無に関わらず書き出される）。したがって `.gitignore` で追跡から外し、Codemagic上の `npx cap sync ios` に正しい `/` 区切りで生成させている。

**追跡しないもの**（すべて `cap sync` が再生成する）
`www/` ／ `ios/App/App/public/` ／ `ios/App/App/capacitor.config.json` ／ `ios/App/CapApp-SPM/Package.swift` ／ `ios/App/Pods/` ／ `node_modules/`

**Info.plist の調整点**

| キー | 値 | 理由 |
|---|---|---|
| `CFBundleDisplayName` | 筋トレ記録 | `capacitor.config.json` の `appName` から自動で入る |
| `ITSAppUsesNonExemptEncryption` | `false` | 宣言しないとビルドを上げるたびに輸出コンプライアンスを聞かれる |
| `UISupportedInterfaceOrientations` | Portrait のみ | 下記 |
| `UISupportedInterfaceOrientations~ipad` | 削除 | iPhone専用にしたため不要 |

**縦向き固定にした理由**：横向き（852×393）でも横スクロールは発生せずレイアウトは崩れないが、タイマー画面で「開始」ボタンが画面外に出てスクロールが必要になることを実測で確認した。トレーニング中に端末を寝かせて意図せず回転するのも避けたいため縦固定にした。

**iPhone専用にした理由**（`TARGETED_DEVICE_FAMILY = 1`）：UIが縦長のスマホ前提（下部タブバー・カード幅）で、iPadでは間延びする。無音カメラは iPad で審査されてリジェクトされた経緯があり、**対応端末を絞れば審査もその端末で行われる**。iPad対応は後から `1,2` に戻せばよい。

**codemagic.yaml**（無音カメラとの違い）

| 項目 | 内容 |
|---|---|
| `bundle_identifier` | `com.kanaeru.kintore` |
| ビルド手順 | `npm install` → **`node build-ios.js`** → `npx cap sync ios` → `use-profiles` → `agvtool` → `build-ipa` |
| `node build-ios.js` を挟む理由 | `www/` は追跡していないため、Codemagic上で生成しないと空になる。ここでクラウド同期のスタブ差し替えと混入チェックも走るので、**同期コードが残っているとこの段階でビルドが止まる** |
| `submit_to_testflight` | `false`（外部テスト審査への提出。前のビルドが審査中だと毎回失敗するため。内部テストは false でも即使える） |

Codemagic側で必要な準備：リポジトリ接続、`Settings > Integrations` に App Store Connect の API キーを **`Codemagic` という名前で**登録（yaml の `integrations` がこの名前を参照している）。無音カメラで登録済みなら共用できる。**ただしAPIキーの共用だけでは足りず、Bundle IDごとの署名ファイル登録が別途必要**（7-14）。

### 7-10. 費用

追加費用なし。Apple Developer Program（$99/年）は無音カメラで支払い済み。Codemagicは無料枠内。Google Sheets/Drive APIも無料枠内（v1.1）。

### 7-11. App名・サブタイトル・キーワード（2026-08-11 決定）

**App Store Connect に入れる3つ**（すべて検索インデックスに載る）

| フィールド | 内容 | 字数 |
|---|---|---|
| App名（30字以内） | `筋トレLog` | 6字 |
| サブタイトル（30字以内） | `セット記録・タイマー・分析・RM計算をこれ1つで` | 24字 |
| キーワード（100字以内・非公開） | `トレーニング,ジム,ウェイト,ベンチプレス,スクワット,デッドリフト,インターバル,1RM,ボリューム,習慣,ログ,ダンベル,バーベル,懸垂,自重,筋肉,体づくり,継続,カレンダー,グラフ` | 94字 |

**キーワード欄に「筋トレ」「記録」「セット」「タイマー」「分析」「RM」を入れていない理由**：App名とサブタイトルに含まれる語は自動的に検索対象になるため、キーワード欄に重ねると100字を無駄にする。これはASOで広く用いられている定石で、Apple公式ドキュメントに明記されたルールではない。なお「Log」は英字なので、カタカナ「ログ」だけはキーワードに残してある（Appleの検索が英字とカタカナを同一視するかが確認できていないため）。

**「筋トレLog」を選んだ理由**
- ユーザーの方針：カタカナ造語（セットログ等）は「分かりづらい」ため不採用。**App名に「筋トレ」を含める**
- Apple の検索API（`itunes.apple.com/search`、日本ストア・上位50件）で58語を調べた結果、この表記は名前に含むアプリが見つからなかった
- カタカナの「筋トレログ」は既に3本ある（Yuki Mayama／株式会社タイニーエフォート ほか）が、**英字 Log なので表記が衝突しない**
- 6文字なのでホーム画面のアイコン下でも省略されない

**調査で潰れた名前**（名前に含むアプリが既にある）

| 語 | 既存アプリ（抜粋） |
|---|---|
| 筋トレ記録 | 上位47件中25件が名前に含む激戦区。俺の筋トレ記録8,006件／筋トレカレンダー4,679件／Gymwork 2,817件／バーンフィット2,499件 |
| 筋トレメモ | **最強の筋トレ管理アプリ-筋トレMEMO（株式会社筋トレMEMO・49,220件）** |
| 筋トレ日記 | 「筋トレ日記」- サクサク入力できる（1,276件）ほか2本 |
| 筋トレ記録メモ | 4本（Mott 33件ほか） |
| その他 | 筋トレノート3本／筋トレ手帳／筋トレ日誌／筋トレ帳／筋トレトラッカー／筋トレ記録ノート2本／筋トレカウンター2本／筋トレヒストリー／筋トレルーティンメモ／Gymaholic筋トレプランナー／筋トレ Diary |

**⚠️「メモ」系は空きでも使わない**：提供元の社名が **株式会社筋トレMEMO** なので、商標登録されている可能性がある（J-PlatPat での確認は未実施）。検索APIでは「筋トレメモ帳」「筋トレメモログ」「筋トレメモノート」が空きと出たが、いずれも避ける。

**この調査の限界**：「Appleの検索APIが返した上位50件に見当たらなかった」という意味であり、同名アプリが存在しないことの証明ではない。**最終確認は App Store Connect でアプリを作成した瞬間**（重複名は登録時に弾かれる）。弾かれた場合の第2候補は `筋トレログブック` / `筋トレ記録帳` / `筋トレDay`（いずれも同じ調査で空き）。

**実装側の対応（2026-08-11 完了）**：ホーム画面に出る表示名 `CFBundleDisplayName` ほか、表に出る名前をすべて「筋トレLog」に統一した（`Info.plist` / `manifest.json` / `index.html` / `capacitor.config.json` / 通知タイトル / CSVファイル名 / `privacy-policy.html` / `support.html`）。ストア上の名前とホーム画面の名前が違うと審査で指摘される可能性があるため。**このとき `build-ios.js` のバージョン抽出が壊れた**：`/筋トレ記録 v([\d.]+)/` とアプリ名をリテラルで書いていたため「不明」に落ちた。表示文言ではなく `class="version"` という構造を目印にする形へ直してある。

### 7-12. App Store Connect 入力内容（2026-08-11 確定）

App Store Connect のアプリ作成は完了済み（Apple ID `6800222286`）。以下は各入力欄にそのまま入れる確定文言。**書いてある機能はすべてコードで実装を確認済み**（存在しない機能を書くと審査で問題になるため、有酸素の記録項目は `cardioEntryHtml`（app.js 1207行）の実装を、CSVインポートは `#importCsvBtn`（index.html 242行）を確認した）。

**「アプリ情報」ページ**

| 欄 | 入力内容 |
|---|---|
| サブタイトル | `セット記録・タイマー・分析・RM計算をこれ1つで`（24字） |
| プライマリカテゴリ | ヘルスケア／フィットネス |
| セカンダリカテゴリ | 設定しない |
| 著作権 | `2026 Kanaeru` |
| 年齢制限指定 | 全項目「なし」→ 4+ |

**「配信」ページ**

| 欄 | 入力内容 |
|---|---|
| キーワード | `トレーニング,ジム,ウェイト,ベンチプレス,スクワット,デッドリフト,インターバル,1RM,ボリューム,習慣,ログ,ダンベル,バーベル,懸垂,自重,筋肉,体づくり,継続,カレンダー,グラフ`（94字） |
| サポートURL | `https://kanaeru-apps.github.io/kintore-log/support.html` |
| マーケティングURL | 空欄 |
| プロモーション用テキスト | 下記（審査なしでいつでも差し替えできる欄） |
| 概要 | 下記 |

プロモーション用テキスト（170字以内・実際は約100字）：
```
ジムでの入力を最短にするために作りました。重量も回数もドラムを回すだけで、キーボードは出てきません。休憩タイマーは画面を消していても鳴ります。アカウント登録は不要、記録はすべて端末の中だけに保存されます。
```

概要（4000字以内）：本文は `store/description-ja.txt` に保存（この欄に直接書くと DESIGN.md が読みにくくなるため）。

**「App Review情報」**
- サインイン：**不要**（「サインインが必要」のチェックを外す）
- 連絡先：support.kanaeru@gmail.com
- 備考（Review Notes）：7-6 の英語版をそのまま使う

**「価格および配信状況」**
- 価格：無料
- 配信国：全世界（日本語のみのアプリだが、海外在住の日本人が使えるよう制限しない）

**「アプリのプライバシー」**
- プライバシーポリシーURL：`https://kanaeru-apps.github.io/kintore-log/privacy-policy.html`
- 収集するデータ：**連絡先情報 › メールアドレス**のみ。目的=アプリの機能／トラッキング=しない／ユーザーIDに紐付け=しない（ご意見フォームの任意入力欄のため）
- それ以外はすべて「収集しない」

### 7-13. スクリーンショット（2026-08-11 作成）

`store/screenshots/6.9inch`（1290×2796・**必須枠**）と `store/screenshots/6.5inch`（1242×2688・任意）に各6枚。

| # | ファイル | コピー | 映している画面 |
|---|---|---|---|
| 1 | `01-input.png` | キーボードは、**出ない。** | 記録画面＋重量ドラム（82.25 / 82.5 / 82.75 が見える） |
| 2 | `02-timer.png` | 画面を消しても、**鳴る。** | インターバルタイマー実行中 |
| 3 | `03-free.png` | 登録なし。広告なし。**課金なし。** | 記録画面（3種目） |
| 4 | `04-calendar.png` | 偏りは、**色でバレる。** | 履歴カレンダーを「脚」で絞った状態 |
| 5 | `05-analytics.png` | 伸びは、**数字で出る。** | 推定1RM推移（73.2→96.6） |
| 6 | `06-rm.png` | 次の重量が、**すぐ出る。** | RM計算機（60kg×8回 → 1RM 70.25kg） |

**この6枚の順序にした理由**：インストールシートに出るのは**最初の3枚だけ**。Appleの検索API（日本ストア）で「筋トレ 記録」上位8本の説明文を調べたところ、激戦区は「習慣化」「記録の簡単さ」「AI分析」で、**8本とも画面ロック中のタイマーに言及していなかった**。そこで競合が触れていない ①キーボードを出さない入力 ②画面ロック中も鳴るタイマー ③完全無料 を1〜3枚目に置いた。4〜6枚目は機能の裏づけ。

**作り方**：`scratchpad/shots_raw.py` で12週分のデモデータを localStorage に流し込み、Playwright（viewport 393×852・scale 3）でアプリ画面だけを撮る。次に `scratchpad/make_shots.py` がその PNG を base64 で埋め込んだ HTML を組み、1290×2796 と 1242×2688 で撮り直す。PIL で文字を描かずHTML/CSSで組んでいるのは、日本語の字詰め・グラデーション・角丸・影・グローをまとめて破綻なく出すため。フォントはローカルの Noto Sans JP Black のみ。

**デモデータで気をつけた点**
- 週4回をちょうど7日周期で置くと、カレンダーの実施日が同じ曜日の縦一列に並んで作り物に見える → 週ごとに0〜2日の揺らぎを入れた
- カレンダーは今月だと今日以降が空になる → 撮影時に先月へ送って丸ごと埋まった状態にした
- RM計算機は初期値（1回）のままだと計算していない画面に見える → 8回に増やしてから撮った
- サイドレイズだけ 0.25kg 刻みの値を入れ、「マイクロプレート対応」が画面から読み取れるようにした

**あえて直さなかった点**（実物と違う印象を与えないため）
- 2枚目のタイマーは実行中に設定UIが隠れる仕様なので画面下半分が空く。実際そう見えるのでそのまま
- 1枚目はドラムのシートが出ると上半分が暗幕（`--overlay`）で暗くなる。iOSのアクションシートと同じ挙動なのでそのまま

### 7-14. Codemagicビルド（2026-08-11 成功）

**結果**：Index 4 / `finished` / 3分11秒 / `App.ipa` 969KB。`Publishing` も完走し App Store Connect へアップロード済み（無音カメラで詰まった422エラーは出なかった）。

**3回失敗した原因：署名ファイルはAppleに作るだけでは足りず、Codemagicに取り込む必要がある**

```
No matching profiles found for bundle identifier "com.kanaeru.kintore" and distribution type "app_store"
```

このエラーは**ビルドマシンが起動する前**に出る（ステップ一覧が1つも表示されない）。Codemagicのバックエンドが署名を事前検証している段階で、ビルド時間は消費されない。

原因の切り分けで確認したこと：

| 確認先 | 状態 | 判定 |
|---|---|---|
| `codemagic.yaml` | 無音カメラとの差分は名前・Bundle ID・`build-ios.js` の3点のみ | 問題なし |
| Apple Developer > Profiles | `Kintore Log App Store Profile` / Active / App Store / `com.kanaeru.kintore` | 問題なし |
| Codemagic > Integrations > Developer Portal | 接続済み（1 key） | 問題なし |
| **Codemagic > Code signing identities > iOS provisioning profiles** | **`zerocam_app_store_profile`（com.kanaeru.zerocam）のみ** | **これが原因** |

**APIキー（`integrations: app_store_connect: Codemagic`）は登録済みでも、プロファイルは Bundle ID ごとに Codemagic 側へ取り込む必要がある。** yaml が automatic code signing の記法（`distribution_type` + `bundle_identifier`）でも、Codemagicは `Code signing identities` に登録されたものを見ている。

**手順**（新しいアプリを追加するたびに必要）

1. Apple Developer > Profiles でプロファイルを作成（証明書はBundle IDに依存しないので既存を使い回せる）
2. Codemagic > `Settings`（チーム設定）> `Code signing identities` > `iOS provisioning profiles`
3. `Fetch profiles` → 該当プロファイルだけチェック → **参照名を入力**（必須。未入力だと "Reference name is required"）→ `Download selected`
4. 命名は `kintore_app_store_profile`（無音カメラの `zerocam_app_store_profile` に合わせた）

証明書は `zerocam-distribution` として登録済みのものが Apple 上の同一 Distribution 証明書（`84L64LR5AM` / 2027-07-17まで）で、Bundle IDに依存しないため追加不要だった。

---

- [x] **Phase 8：ライトモード対応（2026-08-10 実装 / v0.12.0）**

### 8-1. 決めたこと

設定に「外観」を新設し、**システム / ライト / ダーク**の3択で切り替える。**ダークは既存の見た目を1ピクセルも変えない**方針（実際に変わった3箇所は 8-5 に記載）。ライトの配色は4案を作って比較し、**案A「ペーパー × ボルト」**を採用した。白いカードにボルトイエローのボタンが乗る、ダークと地続きの見た目になる。

### 8-2. 仕組み

`<html data-theme="dark|light">` を切り替え、CSSは `:root` と `:root[data-theme='light']` の2つだけを見る。**「システム」を選んでいても属性には dark か light のどちらかが必ず入る**（JSが `matchMedia` で解決してから入れる）ので、CSS側に3つ目の分岐は要らない。

| 場所 | 役割 |
|---|---|
| `index.html` の `<head>` インラインスクリプト | 起動時のちらつき（FOUC）防止。CSSより先に `data-theme` と `theme-color` を決める |
| `app.js` の `initTheme()` / `applyTheme()` | 保存値の読み出し、OS設定の変更監視、切替時の再描画 |
| `localStorage['kintore_theme']` | `system` / `light` / `dark` のいずれか。既定は `system` |

**`<head>` と `app.js` が同じキーと同じ既定値を持っている**点に注意。片方だけ変えると起動時のちらつきが戻る。

### 8-3. 配色設計の核心：`--volt` と `--accent` を分けた

ボルトイエロー `#d7ff3e` は**白背景でコントラスト比 1.18:1** しかなく、ライトでは文字に使えない。しかし塗り（ボタンの面）としては両テーマで使える。そこで用途で変数を割った：

| 変数 | 用途 | ダーク | ライト |
|---|---|---|---|
| `--volt` | **塗り専用**（ボタン・チップ・実施日の面） | `#d7ff3e` | `#d7ff3e`（同じ） |
| `--accent` | **文字・線・アイコン** | `#d7ff3e` | `#5e7a09`（濃いオリーブ・4.9:1） |

ダークでは両方が同じ値なので見た目は完全に不変。**この2つを混ぜると、ライトで文字が消えるか、ボタンが地味になるかのどちらかになる**。新しくボルト色を使うときは、必ずどちらの用途か決めてから選ぶこと。

CSSに直書きされていた93箇所の色は全て変数に寄せた（作業はスクリプトで一括置換し、変数定義行はスキップして `:root` を保護）。

### 8-4. JSが色を書き込む場所への対応

CSS変数だけで色が決まる部分は属性の切り替えだけで追従するが、**JSが色文字列を直接書き込んでいる3箇所**は別途対応が要った。

| 場所 | 問題 | 対応 |
|---|---|---|
| `charts.js` のグラフ（棒・線・点） | `VOLT = '#d7ff3e'` を定数で持っていた | 描画のたび `DB.cssVar('--accent')` で今の値を読む |
| `db.js` の `PART_COLOR`（部位カラー） | ダークは淡色・ライトは濃色と**明暗が逆転する** | `refreshThemeColors()` で中身だけ差し替える。**オブジェクトごと作り直すと、起動時に参照を掴んでいる charts.js が古い色を見続ける** |
| `app.js` の履歴カレンダー（実施日の塗り） | 塗りは部位カラー、文字色はCSSで濃色固定だった | `DB.textOn(色)` がWCAG相対輝度で黒/白を選ぶ。結果、ダークの淡い部位色には黒文字、ライトの濃い部位色には白文字が乗る |

### 8-5. ダークで変わった3箇所（意図的）

ライトで薄すぎたため `--line2`(#3a3f49) → `--faint`(#4a505a) にした結果、ダークでもこの3箇所だけごくわずかに明るくなる。他のプレースホルダーと同じ色に揃う方向の変化なので許容した。

- 重量・回数表示の下線マーカー（`.w-display::after`）
- カレンダーの他月の日付（`.cal-day.other-month`）
- 入力欄のプレースホルダー（`.cf-inputwrap input::placeholder`）

**確認方法**：変更前後のスクリーンショットを画素差分で比較し、グラフ・タイマー画面は完全一致、記録・履歴の差分は上記3箇所のみであることを確認済み。

### 8-6. 未確定：iOSのステータスバー文字色

`index.html` は `apple-mobile-web-app-status-bar-style: black-translucent` を指定している。**ライトモードで白背景に白文字になり、時刻や電池が読めなくなる可能性がある**。

- 対策として `theme-color` はテーマに追従させてある（ライト `#f2f3f5` / ダーク `#0b0c0f`）。iOSがこの値から文字色を決める実装なら、これで解決する**可能性がある**（未検証）
- App Store版は `UIViewControllerBasedStatusBarAppearance = true` のためViewControllerが決める。**iOS本体のダークモード設定に追従する可能性が高い**が、アプリ内で手動でライトを選んだ場合の挙動は実機で見ないと分からない
- どちらも**実機確認が必要**。読めなければ `@capacitor/status-bar` の導入（App Store版）や、ステータスバー領域に背景を敷く（PWA版）で対処する

### 8-7. 検証

Playwrightで自動確認（全てパス）：

| 項目 | 結果 |
|---|---|
| OS=ライトで起動（未設定）→ ライトになる | ✅ |
| 起動後にOS設定が変わったら追従する | ✅ |
| 手動でダークを選んだらOSに追従しなくなる | ✅ |
| 切替がリロードなしで即反映される・保存される | ✅ |
| `theme-color` がテーマで変わる | ✅ |
| カレンダーの塗りセルが `胸`ダーク `#ff8484`＋黒文字 → ライト `#d63b3b`＋白文字 | ✅ |
| グラフの棒が `#d7ff3e` → `#5e7a09` に変わる | ✅ |
| JSエラーが出ない | ✅ |

加えて両テーマ×全6タブ＋タイマー実行中を撮影して目視確認した。

---

- [x] **Phase 9：タイマーのアラーム音・バイブレーション修正（2026-08-11 実装 / v0.13.0）**

### 9-1. 症状

TestFlight版の実機で、**タイマー終了時にアラーム音もバイブレーションも鳴らない**。アプリを開いているときも、閉じている（バックグラウンド／画面ロック）ときも鳴らない。消音スイッチはオフ（消音ではない）、通知の許可ダイアログには「許可」を選んでいる。

### 9-2. 原因

| 症状 | 原因 | 根拠 |
|---|---|---|
| **閉じている／ロック中に鳴らない** | ローカル通知に `sound` を渡していなかった。Capacitor の実装では `sound` 未指定＝`content.sound` が **nil**＝**確実に無音の通知**になる（既定音にはならない） | `LocalNotificationsPlugin.swift:283-285` |
| **バイブが鳴らない** | `navigator.vibrate` は **iOS の WKWebView に存在しない**。実装当初から一度も動いていない | Vibration API 非対応 |
| **開いていても鳴らない** | **未確定**。有力な候補は `OfflineAudioContext` でレンダーした音を `Blob URL` にして `<audio>` に流す経路で、WKWebView では `blob:` の再生が不安定な**可能性がある**。またサイレントスイッチが効く `.ambient` セッションのままだった**可能性もある** | — |

3つ目は再現条件が特定できていない。そのため「原因を1つ直す」のではなく、**不確実な経路を全部やめて、確実に鳴る作りに置き換える**方針にした。

### 9-3. 対策（6点・ユーザー承認済み）

**1. アラーム音を WAV ファイルとして同梱する**

`OfflineAudioContext` → `Blob URL` の経路を全廃し、`<audio src="sounds/beep.wav">` を再生するだけにした。ブラウザが最も確実に鳴らせる形。同時に、**通知音として使うには実ファイルが必須**（`UNNotificationSound` はファイル名しか受け取れない）という要件も同時に満たせる。

- 音源は `tools/gen_alarm_wav.py` が生成する（22050Hz / 16bit / モノラル）
- **これは `js/app.js` の `schedulePattern()` full 版を Python でオフライン再現したもの**。音を変えたいときは JS ではなく **このスクリプトを直して `python tools/gen_alarm_wav.py` を叩き直す**
- 削除した関数：`audioBufferToWavBlob` / `patternDuration` / `renderAlarmBuffer` / `prerenderAlarm` / `alarmRenderCache` / `playBufferOnAudioEl`
- Web Audio 互換の落とし穴：`OscillatorNode` は波形の**振幅が ±1** になるように出る。倍音の合計で割ると矩形波（digital）だけ極端に小さくなるため、`wave_peak()` で**実際の波形のピーク**を求めて割っている

| ファイル | 長さ | ピーク | 鳴る回数 |
|---|---|---|---|
| `beep.wav` | 5.50秒 | 0.40 | 11 |
| `bell.wav` | 6.00秒 | 0.41 | 4 |
| `chime.wav` | 6.00秒 | 0.35 | 9（3回×3音） |
| `digital.wav` | 5.00秒 | 0.22 | 22 |
| `soft.wav` | 5.50秒 | 0.22 | 6 |

**2. ローカル通知に `sound` を指定する（閉じているときの本命）**

```js
if (timerSettings.soundOn) notif.sound = alarmFileName(timerSettings.sound);
```

ただし **通知音のファイルは Capacitor の web 資産の場所からは読めない**。iOS が探すのは **main bundle 直下** か **`Library/Sounds/`** だけで、web 資産は bundle の `public/` 配下に入るため対象外。そこで起動後に `@capacitor/filesystem` で `Library/Sounds/` へコピーする（`ensureNotificationSounds()` → `copySoundToLibrary()`）。コピー済みかは `localStorage['kintore_notif_sound_ver']` と `ALARM_ASSET_VERSION` の比較で判定する。

> **音源を差し替えたら `ALARM_ASSET_VERSION` を上げること。** 上げないと端末に残った古いWAVが使われ続ける。

予約は `Promise.all([ensureNotifPermission(), ensureNotificationSounds()])` の**両方を待ってから**行う。コピー前に予約すると、その回だけファイルが見つからない通知になる。

**3. AVAudioSession を `.playback` にする（開いているときの保険）**

WKWebView の音は既定でサイレントスイッチに従う。`AppDelegate.swift` の起動時と `applicationDidBecomeActive`（電話の着信などで非アクティブにされた後の張り直し）で `.playback` + `.mixWithOthers` を張る。`.mixWithOthers` を付けているので、**Audibleや音楽を止めずに上から重ねて鳴る**（トレーニング中に音楽を止められると困るため）。

**4. バイブを `@capacitor/haptics` に置き換える**

`navigator.vibrate` はネイティブ版では使わない。`Haptics.vibrate({duration:500})` を `setInterval` で 800ms ごと × 7回 ≒ **約5.6秒**繰り返す（アラーム音の長さに合わせた）。ただし **Haptics はアプリが前面にあるときだけ有効**。閉じているときの振動は通知側（iOSの設定）に従う。PWA版は従来どおり `navigator.vibrate`（Androidでは動く）。

**5. 設定に「音の状態」診断を出す**

実機で鳴らなかったときに切り分けられるよう、設定 > タイマー設定に `#soundDiagSection` を追加した（ネイティブ版でのみ表示）。`再生` / `通知音` / `オーディオ` / `通知許可` の4項目に結果を出す。

**6. 「消音モードでも鳴らす」トグル**

既定 **ON**（=`.playback`）。OFFにすると `.ambient` になり消音スイッチに従う。JS から `AlarmAudio.setIgnoreSilentMode({value})` を呼ぶ。**起動直後は WebView がまだ動いていないので JS から受け取れない**ため、`UserDefaults`（キー `kintore_ignore_silent`）に写して AppDelegate がそれを読む。

### 9-4. 自前プラグインの登録方法（重要）

`AlarmAudioPlugin` はアプリターゲット側にあるため、**`capacitor.config.json` の `packageClassList` には載せられない**（あのファイルは `cap sync` が毎回作り直す）。そのため手動登録している：

```
Main.storyboard の customClass = MainViewController（customModule="App"）
        ↓
MainViewController: CAPBridgeViewController
        ↓ capacitorDidLoad() をオーバーライド（bridge生成直後・WebView読込前に呼ばれる）
bridge?.registerPluginInstance(AlarmAudioPlugin())
```

**この3つは連動している。** Storyboard の `customClass` を戻すとプラグインが消えて `AlarmAudio` の呼び出しが無言で失敗する（JS側は `nativePlugin()` が undefined を返して素通りするだけなので、エラーも出ない）。

`AlarmAudioPlugin` は `CAPBridgedPlugin` プロトコル（`identifier` / `jsName` / `pluginMethods` の3プロパティ）を満たす必要がある。

### 9-5. 検証

| 項目 | 結果 |
|---|---|
| `node --check` （app.js / sw.js / build-ios.js） | ✅ |
| 削除した関数への残存参照 | ✅ ゼロ |
| 生成WAVの長さ・ピーク・鳴る回数が設計値と一致 | ✅ 5音すべて（上表） |
| `node build-ios.js` で `www/sounds/` に5本コピー | ✅ |
| `npx cap sync ios` でプラグイン3つ検出（filesystem / haptics / local-notifications） | ✅ |
| `ios/App/App/public/sounds/` に5本同梱 | ✅ |
| Playwright：音色タップで `currentSrc` が `sounds/bell.wav`・`duration=6`・`el.error` なし | ✅ |
| Playwright：`sounds/bell.wav` が HTTP 206・失敗リクエストなし・JSエラーなし | ✅ |
| Playwright：タイマー終了で「TIME UP」まで到達 | ✅ |

**未検証（実機でしか確認できない）**：実際に音が出るか、閉じているときに通知音が鳴るか、Haptics が振動するか。→ TestFlight で確認する。

### 9-6. 実装中に自分で見つけて直した点

- **解錠再生と試聴再生の競合**：初回タップ時、無音WAVでの解錠 `play()` が非同期に解決した後の後始末が `el.pause()` を呼ぶため、その間に始まった試聴が一瞬で止まる。→ 後始末の冒頭で `if (el.getAttribute('data-sound')) return;` として、本命の再生が始まっていたら触らないようにした
- **「コピー未完了なら `sound` を指定しない」は誤り**：無指定は既定音ではなく**確実な無音**（`LocalNotificationsPlugin.swift` は `sound` キーが無いと `content.sound` に何も入れない＝`nil` のまま）
  - ⚠️ このとき「ファイル名を渡しておけば、万一見つからなくても iOS が既定音を鳴らす**と考えられる**」と書いたが、これは**確証のない推測**だった。Phase 9.6 の症状（バナーは出るのに無音）と整合しないため、**推測として扱い、実機で確かめられる形に作り直した**

---

## Phase 9.5：バックグラウンド通知が出ない問題の切り分け（2026-08-11 / v0.13.1）

### 9.5-1. 症状

v0.13.0 を TestFlight で実機確認したところ、**アプリを開いている間はアラームが鳴るようになった**（Phase 9 の対策は成功）。
しかし**アプリを閉じて他のアプリを操作していると、音どころか通知バナーすら出ない**。消音スイッチは着信音側。

→ 音の問題ではなく、**ローカル通知そのものが届いていない**。

### 9.5-2. なぜなぜ分析（5回）

| # | 問い | 答え |
|---|---|---|
| 1 | なぜバナーが出ない？ | 予約が成立していないか、通知が許可されていないかのどちらか |
| 2 | なぜどちらか分からない？ | `ln.schedule()` の失敗を `.catch(function () { /* noop */ })` で捨てていたから |
| 3 | なぜ捨てた？ | 「通知は補助機能だからアプリを止めない」と考えて握り潰した |
| 4 | なぜそれが誤り？ | 画面ロック中の通知は**このアプリ最大の売り**であり、補助ではなく主機能 |
| 5 | **本質は？** | **実機でしか検証できない主機能を、失敗が観測できない作りのまま実機に出した** |

Phase 9 で追加した診断も、**音ファイルのコピー結果しか見ておらず、予約の成否を見ていなかった**。ここが根本の穴。

### 9.5-3. 静的検証で分かったこと（コードの誤記ではない）

| 確認した点 | 結果 |
|---|---|
| `sound` がプラグインに渡っているか | ✅ `LocalNotificationsPlugin.swift` が `UNNotificationSound(named:)` に渡している |
| `directory:'LIBRARY'` の解決先 | ✅ `libraryDirectory`（＝`Library/Sounds/` に置ける） |
| `recursive: true` | ✅ `includeIntermediateDirectories: true`（`Sounds/` は自動作成） |
| 予約の呼び出し箇所 | ✅ 開始・再開・時間延長で取り直し、停止・終了で取り消し。抜けなし |
| 前面時にバナーが抑止される設定 | ✅ 既定で `[.badge, .sound, .banner, .list]`。抑止していない |
| `UIBackgroundModes` | ✅ 未設定＝背面ではJSが止まる。`finishTimer()` が背面で予約を消す事故は起きない |

**残る候補で静的に潰せないもの**：
- 通知が実際には許可されていない（`checkPermissions()` が `denied`）
- `ln.schedule()` が reject している（理由不明）
- `schedule.at` の `Date` がブリッジで `NSDate` にならず、**trigger が nil ＝ 即時配信**されている
  （`handleScheduledNotification` は `at` が `NSDate` でなければ nil を返し、`UNNotificationRequest(trigger: nil)` は即座に配信される。**`schedule()` は成功したまま**なので気づけない）

### 9.5-4. 対策：予約を観測できるようにする

1. `soundDiag` に `sched`（予約の成否と理由）と `pending`（OSが預かっている予約）を追加
2. `scheduleTimerNotification()` の**無言の早期 return 3箇所**（プラグイン無し／システム通知OFF／許可なし）に理由を記録
3. `ln.schedule()` の空 `catch` を、**理由を記録する catch** に変更
4. 予約成功後に `ln.getPending()` を叩き、**OSに本当に入ったか**を件数と時刻で表示
5. 設定 > タイマー設定 に**「通知テスト（10秒後）」ボタン**を追加。タイマーを5分回さずに何度でも試せる
6. 許可が下りていない場合は、テストボタンが `設定 > 通知 > 筋トレLog` を開くよう文言で案内

**`予約` と `OS予約` を分けたのが肝**。この2つの組み合わせで原因が一意に決まる：

| 予約 | OS予約 | 意味 |
|---|---|---|
| `OK 12:34:56 / beep.wav` | `1件: #1 12:34:56` | 予約は正常。原因はiOS側の通知設定（バナーOFF・集中モード・通知の要約） |
| `OK ...` | `0件（OSに予約なし）` | **trigger が nil ＝ 即時配信された**。`schedule.at` の日時が渡っていない |
| `NG: 通知が許可されていない` | `0件` | 許可が下りていない。iOSの設定でONにする |
| `NG: <理由>` | `0件` | プラグインが予約を拒否した。理由がそのまま出る |
| `—（システム通知がOFF）` | — | アプリ内のトグルがOFF |

### 9.5-5. 検証（Playwright＋Capacitorモック・4パターン）

| モック条件 | 予約 | OS予約 | 判定 |
|---|---|---|---|
| 許可あり・予約成功 | `OK 21:44:32 / beep.wav` | `1件: #1 21:44:32` | ✅ |
| 許可なし（denied） | `NG: 通知が許可されていない` | `0件（OSに予約なし）` | ✅ |
| `schedule()` が reject | `NG: Scheduled time must be *after* current time` | `0件（OSに予約なし）` | ✅ |
| 成功したがOSに残らない | `OK 21:44:48 / beep.wav` | `0件（OSに予約なし）` | ✅ 切り分けできる |
| ブラウザ版 | 診断セクションは `hidden` | — | ✅ |

`node --check` ✅ ／ `node build-ios.js` ✅ ／ `npx cap sync ios` でプラグイン3つ検出 ✅ ／ JSエラーなし（`file://` での `fetch` 不可のみ。実機は `capacitor://` なので該当しない）

### 9.5-6. 実機での結果（2026-08-11）

**解決**。アプリを閉じて他のアプリを操作中に、本番タイマーの終了で通知バナーが届いた。
→ ただし**音は鳴らなかった**。これは Phase 9.6 で扱う。

---

## Phase 9.6：通知は出るのに音が鳴らない（2026-08-12 / v0.13.2）

### 9.6-1. 症状

アプリを閉じた状態で本番タイマーを回すと、終了時に**通知バナーは出る**。しかし**音が鳴らない**。
アプリを開いている間のアラーム音は v0.13.0 以降ちゃんと鳴っている。

→ 通知の**予約・配信は成立**していて、**音だけが付いていない**。

### 9.6-2. なぜなぜ分析（5回）

| # | 問い | 答え |
|---|---|---|
| 1 | なぜ通知に音が付かない？ | `UNNotificationSound(named: "beep.wav")` が音を見つけられていない可能性が高い |
| 2 | なぜ見つからない？ | iOS が探すのは **bundle 直下** と **`Library/Sounds/`** の2箇所だけ。web資産は bundle の `public/` 配下なので対象外 |
| 3 | なぜ `Library/Sounds/` に無い？ | JS が `fetch → arrayBuffer → btoa → Filesystem.writeFile` という長い経路でコピーしていて、途中で失敗しても無言だった |
| 4 | なぜ失敗に気づけない？ | 診断欄が「コピーしたつもり（localStorage のフラグ）」しか見ておらず、**実ファイルの有無を見ていなかった** |
| 5 | **本質は？** | **成功しか確認できない指標を「確認済み」として扱った**。Phase 9.5 の教訓（失敗を観測できるようにする）を、音ファイルの側にだけ適用し損ねていた |

### 9.6-3. 静的に確認できたこと

| 確認した点 | 結果 |
|---|---|
| WAV のフォーマット | ✅ RIFF/PCM・16bit・モノラル・22,050Hz・約5.5秒。`UNNotificationSound` の対応範囲内（30秒以内・リニアPCM） |
| `sound` が渡っているか | ✅ 予約欄に `OK 12:34:56 / beep.wav` と出ており、JS→プラグインまでは届いている |
| `sound` を省いたときの挙動 | ✅ `content.sound` が `nil` のまま＝**確実な無音**。省略は解決にならない |
| bundle 内の `public/sounds/` | ✅ 5本入っている。ただし**通知からは参照できない場所** |

**静的に潰せない候補**（実機でしか分からない）：
- `Library/Sounds/` にコピーできていない
- iPhone の「設定 > 通知 > 筋トレLog」で**サウンドだけオフ**になっている

### 9.6-4. 対策

**(a) 経路をなくす（本命）**
`sounds/*.wav` を **Xcode の Resources に個別ファイルとして追加**し、**bundle 直下**に入れる。
こうすると `UNNotificationSound(named: "beep.wav")` がそのまま見つけられ、**実行時のコピーが一切不要**になる。

- `ios/App/App.xcodeproj/project.pbxproj` に `PBXFileReference` × 5（`sourceTree = SOURCE_ROOT` / `path = ../../sounds/*.wav`）と `PBXBuildFile` × 5 を追加し、Resources ビルドフェーズに載せた
- 個別ファイル参照は bundle 直下にフラットに置かれる（フォルダ参照＝青フォルダにすると `sounds/` サブディレクトリになり、通知からは見つからない）
- リポジトリ直下の `sounds/` をそのまま参照するのでコピー手順が増えない
- `xcode` パッケージで**パースし直して壊れていないことを検証**した（Resources に5本が載っていること・パスの解決先を確認）

**(b) 経路を短くする（予備）**
それでも `Library/Sounds/` が要る古いビルド向けに、`AlarmAudioPlugin.installSounds` を追加。
bundle → `Library/Sounds/` の**ネイティブ1発コピー**にした（JS の fetch→base64→Filesystem は予備として残す）。
既存ファイルは `removeItem` せず**上書き**する（CLAUDE.md の削除禁止方針）。

**(c) 原因を一意に特定できるようにする**
`AlarmAudioPlugin` に確認用メソッドを追加し、設定 > タイマー設定 の診断に2行足した：

| 追加メソッド | 返すもの | 診断欄 |
|---|---|---|
| `soundFiles({names})` | bundle 直下に何本あるか＋`Library/Sounds/` の実ファイル名とサイズ | 「通知音の実ファイル」 |
| `notificationSettings()` | `soundSetting` / `alertSetting` / `lockScreenSetting` / `alertStyle` / 承認状態 | 「iPhone側の通知設定」 |

Capacitor の `checkPermissions()` は `authorizationStatus` しか返さないため、**「許可はしたがサウンドだけオフ」を区別できなかった**。これを見えるようにした。

**原因の判定表**：

| 通知音の実ファイル | iPhone側の通知設定 | 原因 |
|---|---|---|
| `アプリ本体に5本 ／ Library: …` | `音オン` | 音ファイル・OS設定とも正常。ここまで来て無音なら別要因 |
| `0件（通知音のファイルが無い＝無音になる）` | `音オン` | **同梱もコピーも失敗**。バナーだけ出て無音になる |
| `アプリ本体に5本 …` | `音オフ` | **iPhoneの設定でサウンドだけオフ**。設定 > 通知 > 筋トレLog でONにする |
| `アプリ本体になし ／ Library: …` | `音オン` | 旧ビルド。コピー経路で動いている（正常だが本命ではない） |

**(d) 版ずれの修正**
`ALARM_ASSET_VERSION` を書き込むだけで**読んでいなかった**ため、音源を差し替えても古いファイルが残り続ける状態だった。版が一致しないときは再コピーするようにした。

### 9.6-5. 検証（Playwright＋Capacitorモック・7パターン）

| モック条件 | 音の準備 | iPhone設定 | 通知音の実ファイル | 判定 |
|---|---|---|---|---|
| 正常（同梱あり） | `OK (アプリ本体に同梱)` | `音オン` | `アプリ本体に5本 ／ Library: …` | ✅ |
| 許可なし | `OK (アプリ本体に同梱)` | `音オン（拒否）` | 同上 | ✅ |
| `schedule()` が reject | 同上 | 同上 | 同上 | ✅ |
| 予約がOSに残らない | 同上 | 同上 | 同上 | ✅ |
| **音ファイルが無い** | `NG: コピー失敗 …` | `音オン` | `0件（通知音のファイルが無い＝無音になる）` | ✅ |
| **iPhone側でサウンドだけオフ** | `OK (アプリ本体に同梱)` | `音オフ` | `アプリ本体に5本 …` | ✅ |
| 旧ビルド（同梱なし） | `OK (ネイティブでコピー)` | `音オン` | `アプリ本体になし ／ Library: …` | ✅ |
| ブラウザ版 | 診断セクションは `hidden` | — | — | ✅ |

`node --check` ✅ ／ `node build-ios.js` ✅ ／ `npx cap sync ios` ✅ ／ `project.pbxproj` の再パース ✅ ／ JSエラーなし（`file://` での `fetch` 不可のみ。実機は `capacitor://` なので該当しない）

### 9.6-6. 実機で見るところ

TestFlight で v0.13.2 を入れたら、**設定 > タイマー設定 > 「音の状態」** の次の2行を読む：

- **通知音の実ファイル** … `アプリ本体に5本` と出ていれば (a) が効いている
- **iPhone側の通知設定** … `音オン` になっているか

その上で「通知テスト（10秒後）」を押し、ホーム画面に戻って待つ。

## Phase 9.7：v0.13.2 で通知が一切来なくなった（2026-08-12 / v0.13.3）

### 9.7-1. 症状

ユーザー報告：「他のアプリを開いてると通知来なくなったよ」

v0.13.1 では**出ていた**バックグラウンド通知が、v0.13.2 で**一切出なくなった**。
「通知テスト」を押しても来ない。9.6 の対策を入れたビルドで発生したので、**9.6 の変更が原因のリグレッション**。

### 9.7-2. なぜなぜ分析（5回）

1. **なぜ通知が来ないのか** → `ln.schedule()` が呼ばれていないから
2. **なぜ呼ばれないのか** → `scheduleTimerNotification()` が
   `Promise.all([ensureNotifPermission(), ensureNotificationSounds()])` の**完了を待ってから**
   `schedule()` を呼ぶ作りで、この `Promise.all` が解決しないから
3. **なぜ解決しないのか** → `ensureNotificationSounds()` の中で v0.13.2 に新設した
   `copyViaNative()`（＝ネイティブの `installSounds` を呼ぶ）が返ってこないと、そこで止まるから。
   さらに v0.13.2 で `ALARM_ASSET_VERSION` を `'1'`→`'2'` に上げたため
   「コピー済み」の判定が全端末で外れ、**必ずこの新経路を通る**ようになっていた
4. **なぜ止まったまま気付けないのか** → どのネイティブ呼び出しにも**時間切れが無い**から。
   「失敗」ではなく「無反応」なので診断欄にも何も出ず、画面上は「予約中…」ですらなく初期値のまま
5. **なぜそんな作りになっていたのか** → **音の準備という補助処理を、通知の予約という本体機能の完了条件にしてしまった**から。
   音は「付けば嬉しい」もので、無くても通知は出るべきだった

**本質**：失敗しうる補助処理を、本体機能の前提条件に置いてはいけない。
そして**外部（ネイティブ／OS）に問い合わせる処理には必ず時間切れを付ける**。

### 9.7-3. 対策

**(a) 音の準備と通知の予約を切り離す**

`Promise.all` は残すが、それぞれを `withTimeout()` で包み、**時間切れでも reject せず既定値で解決**させる。
音の準備は 2 秒（`SOUND_WAIT_MS`）で見切り、許可の確認だけは
ユーザーがダイアログに答えるのを待つ可能性があるので 20 秒（`PERM_WAIT_MS`）。
**音の準備がどうなろうと `schedule()` は必ず呼ばれる。**

```js
function withTimeout(promise, ms, fallback) { /* 時間切れでも fallback で resolve */ }

Promise.all([
  withTimeout(ensureNotifPermission(), PERM_WAIT_MS, null),
  withTimeout(ensureNotificationSounds(), SOUND_WAIT_MS, false)  // ← 待ち続けない
]).then(function (r) { ... ln.schedule({ notifications: [notif] }) ... });
```

**(b) 同期例外を Promise に畳む**

`alarm.soundFiles(...)` が**同期で** throw すると `.then().catch()` に到達せず呼び出し元へ投げ出され、
`Promise.all` の引数を組み立てている最中に例外になる（＝(a) の時間切れすら効かない）。
すべてのネイティブ呼び出しを `Promise.resolve().then(function(){ return alarm.xxx(); })` の形にした。
併せて `scheduleTimerNotification` / `runNotifTest` を Inner 関数に分離し、外側を `try/catch` で包んだ。

**(c) キャッシュした Promise を reject させない**

`notifSoundsPromise` は一度 reject すると以後**ずっと**失敗を返す。
`.catch()` で必ず `false` に畳んで解決させる。

**(d) アプリ内エラーを画面に出す**

ネイティブ版はブラウザの開発者ツールが使えないため、JS がどこかで落ちても実機からは何も分からない。
`window.onerror` / `unhandledrejection` と各 `.catch()` で拾い、
診断の **「アプリ内エラー」** 欄（`#diagErr`）に出す。

**(e) 診断欄が固まらないようにする**

OS への問い合わせが返ってこないと欄が「確認中…」のまま残る。
`fillDiag()` で 4 秒（`DIAG_WAIT_MS`）で見切り、**「確認が返ってきません（時間切れ）」**と明示する。

### 9.7-4. 検証（Playwright＋Capacitorモック・9パターン）

**見るのは「通知の予約」と「OS予約」の2列**。音の状態がどうであれ予約は通ること。

| モック条件 | 通知の予約 | OS予約 | 判定 |
|---|---|---|---|
| 正常（同梱あり） | `OK …/ beep.wav` | 1件 | ✅ |
| 許可なし | `NG: 通知が許可されていない` | 0件 | ✅（正しい失敗） |
| `schedule()` が reject | `NG: Scheduled time must be *after* current time` | 0件 | ✅（正しい失敗） |
| 予約がOSに残らない | `OK …` | 0件 | ✅ |
| 音ファイルが無い | `OK …` | 1件 | ✅ |
| iPhone側でサウンドだけオフ | `OK …` | 1件 | ✅ |
| 旧ビルド（同梱なし） | `OK …` | 1件 | ✅ |
| **hang（ネイティブが返事を返さない＝v0.13.2の症状）** | `OK …` | **1件** | ✅ 実ファイル欄 `確認が返ってきません（時間切れ）` |
| **throw（ネイティブ呼び出しが例外）** | `OK …` | **1件** | ✅ アプリ内エラー欄 `通知音の確認: soundFiles is not a function` |
| ブラウザ版 | 診断セクションは `hidden` | — | ✅ |

`node --check` ✅ ／ `node build-ios.js` ✅ ／ `npx cap sync ios` ✅

### 9.7-5. 実機で見るところ

1. まず**通知が来ること**（設定 > タイマー設定 > 「通知テスト」の「試す」→ ホーム画面に戻って10秒待つ）
2. 来たら**音が鳴るか**。鳴らなければ「音の状態」の3行を読む
   - **通知音の実ファイル** … `アプリ本体に5本` なら 9.6 の対策(a)が効いている
   - **iPhone側の通知設定** … `音オン` か `音オフ` か
   - **アプリ内エラー** … `なし` 以外ならその文言が原因

---

## Phase 9.8：通知まわりの総点検（2026-08-17 / v0.13.4）

v0.13.3 を出す前に「他のアプリを開いていると音も通知も来ない」という報告があったため、
通知に関わるコードを端から読み直した。9.7 で直したのは症状の中心だが、
**同じ根から出ている問題があと5つ残っていた**。

### 9.8-1. 見つかった6件

| # | 症状 | 原因 | 重さ |
|---|---|---|---|
| 1 | タイマー開始直後に他アプリへ切り替えると通知が来ない | 予約が「許可の確認」「音の準備」の完了を待ってから走る。iOSは背面でJSを止めるので、待っている間に閉じられた回は `schedule()` に到達しない | 最重 |
| 2 | まれに予約が消える | 予約の直前に `cancel()` を投げっぱなしにしていた。完了を待っていないので順序が入れ替わると、予約したばかりの通知を自分で消す | 高 |
| 3 | アプリを開いたまま待つとバナーが出ない | 終了時に必ず `cancel()` していた。`cancel` が消すのは未配信の予約なので、OSが今出そうとしているバナーを叩き落としていた | 高 |
| 4 | 前面で終わると音が二重に鳴る | `presentationOptions` 未設定。Capacitorの既定は `sound` を含むため、アプリ内アラームと通知音が重なる | 中 |
| 5 | バイブが途中で切れることがある | Haptics(CoreHaptics)はエンジンをその場で作って捨てる作りで、再生の途中で解放されうる | 中 |
| 6 | 「消音モードでも鳴らす」が通知音に効かない | iOSの通知音はアプリのオーディオ設定と無関係にOSが鳴らす。構造的に届かないのに、UIの文言はそう読めなかった | 低(UI) |

### 9.8-2. なぜなぜ分析（5回）— 1番について

1. **なぜ他アプリを開くと通知が来ないのか** → 予約がOSに届く前にJSが止まるから
2. **なぜ止まる前に予約できないのか** → 予約が「許可確認」と「音の準備」の完了を待つ作りだったから
3. **なぜ待つ作りにしたのか** → 「コピーが終わる前に予約すると、その回だけ音の無い通知になる」と考えたから
4. **なぜその心配が要らないのか** → v0.13.2 で通知音をアプリ本体に同梱した時点で、`buildNotif()` は準備状況を見ずにファイル名を書くだけで音が鳴るようになっていた。**待つ理由は既に消えていたのに、待つコードだけが残っていた**
5. **なぜ気付かなかったのか** → 同梱方式に変えたとき、その変更が「準備待ち」を不要にしたことを確かめず、新しい経路を足しただけで古い前提を撤去しなかった

**本質**：前提が変わったのに、その前提の上に建てた待ち合わせを撤去しなかった。
そのうえ「待つこと自体のリスク（背面でJSが止まる）」を一度も評価していなかった。

> **教訓**：仕組みを入れ替えたら、**その仕組みのために置いた回避策を消す**ところまでが変更。
> 足すのは気づくが、要らなくなったものを外すのは意識しないと落ちる。

### 9.8-3. 対策

- **(a) 予約は「まず出す、あとで直す」**（1・2）
  `scheduleTimerNotification()` は何も待たずに `pushNotif()` で予約する。
  許可の確認はそのあとに走らせ、取れたら**同じidで出し直す**（`add()` は同一idを置き換えるので二重にならない）。
  予約前の `cancel()` は削除した。同一idなら置き換わるので、そもそも要らなかった。
- **(b) 背面に回る瞬間の保険**（1）
  `visibilitychange`(hidden) で、動作中のタイマーがあれば予約を出し直す。
  開始時の予約が届いていなくてもここで拾える。
- **(c) 時間どおりに終わったときは取り消さない**（3）
  `finishTimer()` の `cancel()` は「終了時刻まで1.5秒より手前」のときだけ。
  代わりに配信済みの通知は、次にタイマーを始めるとき／リセット時に `removeDeliveredNotifications` で片づける。
- **(d) 前面では通知音を鳴らさない**（4）
  `capacitor.config.json` に `presentationOptions: ["banner","list","badge"]`。
  前面＝アプリ内アラーム音＋バナー、背面＝通知音。役割を分けた。
- **(e) 振動は自前プラグインを本命に**（5）
  `AlarmAudioPlugin.vibrate` を追加し `AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)` で鳴らす。
  Capacitorの `registerPlugin` はプロキシを返すため、ネイティブ登録が漏れていてもJSからは関数に見える。
  したがって**「あるか」ではなく「呼んで失敗したか」で** Haptics に切り替える。
- **(f) 文言を実態に合わせる**（6）
  「消音モードでも鳴らす」→「アプリを開いているときのアラーム音のみ。閉じているときの通知音は iPhone の設定に従います」。

### 9.8-4. 日付の受け渡し（確認だけ・変更なし）

`LocalNotificationsPlugin.handleScheduledNotification` は `schedule["at"] as? NSDate` を見ており、
**ISO文字列で渡すと nil → trigger なし → 即時配信**になる。
iOSの `postToNative` は `webkit.messageHandlers.bridge.postMessage(data)` にオブジェクトをそのまま渡し、
WebKitの構造化クローンが `Date` を `NSDate` にするため、**`at` は Date のまま渡すのが正しい**（現行どおり）。
JSON.stringify するのは Android 経路だけ。ここを「文字列の方が安全」と直すと即時配信に化けるので触らないこと。

### 9.8-5. 検証（Playwright＋Capacitorモック）

**予約が確実に出るか**（10パターン。`permhang`＝許可の確認が返ってこない、を追加）

- 全10パターンで **START直後（50ms後）に `schedule` 済み**を確認
- 全10パターンで **`cancel` が予約より先に走っていない**ことを確認
- とくに `permhang` は、v0.13.3 でも残っていた穴（許可の確認待ち）が塞がったことの直接の証拠

**終了時のふるまい**（1分→−30秒×2で終了させる）

| 確認 | 結果 |
|---|---|
| 開始時に配信済み通知を片づける | `removeDelivered:id=1` |
| 時間どおり終わったとき予約を取り消さない | `cancel` が出ない |
| 自前プラグインが動く端末 | `aa.vibrate` のみ（Hapticsを呼ばない） |
| 自前プラグインが未登録の端末 | `aa.vibrate` 1回失敗 → `haptics` に切替 |

### 9.8-6. 実機で見るところ

1. **タイマーを開始してすぐホーム画面に戻る** → 時間どおりに通知が来るか（今回の本命）
2. 来たら**音が鳴るか**、**振動するか**
3. **アプリを開いたまま**待つ → バナーが出るか、音が二重に鳴っていないか
4. 「消音モードでも鳴らす」をオフ＋本体を消音 → アプリを開いている間のアラームが鳴らないこと
   （閉じているときの通知音は iPhone 設定側なので、ここでは変わらない）

### 9.8-7. 未処理（ユーザー判断待ち）

バックグラウンドで通知を受けたあとにアプリを開くと、`finishTimer()` が改めて走って
**アラーム音とバイブがもう一度鳴る**。通知で既に知らされているぶん冗長だが、
「消音にしていて通知に気づかなかった」場合は開いたときの音が唯一の合図になる。
どちらを採るかは仕様の選択なので、今回は現状のままにした。

## Phase 9.9：原因を画面に出す（2026-08-17 / v0.13.5）

### 9.9-1. 実機の結果と、そこから確定したこと

v0.13.4 を実機で確認した結果：

| 試したこと | 結果 |
|---|---|
| 開始直後にホーム画面へ戻る | **通知が来ない・音も鳴らない** |
| アプリを開いたまま終了を待つ | 振動する・アプリ内のアラーム音は鳴る。**バナーは出ない**。音は二重にならない |

ここから確定できること：

- JS は動いている。`AlarmAudioPlugin` も登録されている（振動が動いているため）
- **前面でも背面でもローカル通知が1件も出ていない**
- 「音が二重にならない」は `presentationOptions` が効いた証拠**ではない**。
  そもそもバナーが出ていないのだから、二重にならないのは当たり前

`presentationOptions` は前面表示だけを決める設定なので、**背面で出ないことを説明できない**。
コード側は次をすべて実装レベルで確認し、経路は生きていることを確かめた：

- `AppDelegate` は `UNUserNotificationCenter` の delegate を横取りしていない
- iOS の `postToNative` は `postMessage(data)` にオブジェクトをそのまま渡すため、
  JS の `Date` は WebKit の構造化クローンで `NSDate` になる。Capacitor 側も `extension Date: JSValue {}` で受けられる
  → `schedule.at` は `handleScheduledNotification` の `as? NSDate` を通り、トリガが正しく作られる
- `presentationOptions` の `banner` / `list` / `badge` は `LocalNotificationsHandler` が解釈できる綴り
- Service Worker は `location.protocol` が http のときだけ登録するので、アプリ版では古い資産が出ることはない

したがって残る原因は次のどれか。**いずれも端末側の状態**で、コードを読んでも分からない：

1. 通知が許可されていない（拒否した／許可ダイアログに答えないままアプリを閉じた）
2. iPhone 側でバナーやサウンドが個別にオフ
3. 「仮承認（静かに配信）」になっている
4. **「通知の要約」に入れられていて、終了時刻ではなくまとめ配信の時間まで保留されている**
5. 入れたビルドが v0.13.5 でない

### 9.9-2. なぜここまで分からなかったのか（なぜなぜ5回）

1. なぜ原因が分からない → 通知が出せない状態でも、アプリが**何も言わずに黙って動く**から
2. なぜ黙っている → 許可が無いときの分岐が `soundDiag.sched` に文字列を入れるだけだったから
3. なぜそれで足りると思ったか → 診断欄を作ったとき「見に行けば分かる」で満足したから
4. なぜ見に行けば分かるでは足りないのか → 診断欄は設定 > タイマー > 音の状態と**3階層下**にあり、
   そこを見るという発想自体が、原因を疑って初めて出てくるものだから
5. なぜ最初からタイマー画面に出さなかったのか → 「診断は開発者が見るもの」と考えていた。
   実際には**実機でしか再現しない不具合は、使う人が見るものでなければ役に立たない**

**本質**：実機でしか起きない不具合の計器は、設定の奥ではなく**その機能を使う画面**に置く。

### 9.9-3. 直したこと

**(a) タイマー画面の一番上に警告を出す**（`#trNotifWarn`）

通知が出せない状態のときだけ赤い帯を出す。出す条件と文言：

| 状態 | 出る文言 |
|---|---|
| プラグインが読み込めていない | 通知の仕組みを読み込めていません |
| 許可が `denied` | 通知が許可されていません |
| 許可が未確定 | 通知の許可がまだ済んでいません |
| 仮承認 | 通知が「静かに配信」になっています |
| iPhone側でバナーがオフ | バナー表示がオフです |
| iPhone側でサウンドがオフ | iPhone側で通知音がオフです |
| 通知の要約に入っている | 「通知の要約」に入っています |

「システム通知」を自分でオフにしている場合は出さない（本人の意思なので）。
タイマー画面を開いた時点・開始した時点・他アプリから戻った時点で取り直す。

**(b) 警告を押すと iPhone の設定を開く**（`AlarmAudioPlugin.openSettings`）

一度「許可しない」を選ぶと、アプリからは二度と確認ダイアログを出せない
（iOS はインストールごとに一度しか出さない）。設定アプリへ送るのが唯一の復旧手段なので、
`UIApplication.openSettingsURLString` を開く一手をアプリ内に用意した。

**(c) 許可の確認を「一度きり」から「今出している最中か」に変えた**

これまでは `notifPermissionAsked` を立てたら、そのアプリ起動中は二度と許可を求めなかった。
ところが確認ダイアログを出した直後にホーム画面へ戻られると、
返事が返らないまま JS も止まるため、**フラグだけが立って永久に聞けない**状態になる。
フラグの意味を「今ダイアログを出している最中か」に変え、
前面に戻ったとき（`visibilitychange` の visible）に必ず解くようにした。
iOS はシステムのダイアログを一度しか出さず、答え済みなら結果だけ即返すので、聞き直しても画面はうるさくならない。

**(d) 診断に「配信された通知」を追加**（`getDeliveredNotifications`）

これが**原因を二分する唯一の手がかり**になる。

| 通知の予約 | OSに入っている予約 | 配信された通知 | 読み方 |
|---|---|---|---|
| `NG: …` | — | — | その文言がそのまま原因 |
| `OK …` | `0件` | `0件` | 予約がOSに入っていない。時刻の受け渡しかプラグインの問題 |
| `OK …` | `1件: #1 hh:mm:ss` | `0件` | 予約は入っているが配信されていない。**許可か iPhone 側の設定** |
| `OK …` | `0件` | `1件: #1` | 配信は済んでいる。**画面に出ていないだけ**（要約・集中モード・バナーオフ） |

※「配信された通知」は通知センターに残っているものを数えるので、手で消したあとは `0件` に戻る。

**(e) 診断の「iPhone側の通知設定」に「要約」を追加**

`UNNotificationSettings.scheduledDeliverySetting`（iOS 15+）を読む。
許可もサウンドもオンなのに来ない場合、ここが `オン` なら原因はこれ。

### 9.9-4. 検証

`check_warn.py`（Capacitor をモックして 8 パターン）
— 正常時に警告が出ないこと、6 種類の異常でそれぞれ正しい文言が出ること、
押すと `openSettings` を呼ぶこと、「配信された通知」が 0件 / 1件 を出し分けること、
**許可の確認が返ってこないまま前面に戻ったら次の開始でもう一度確認できること**を確認。全パターン合格。
`check_notif_diag.py` / `check_finish.py`（9.7・9.8 の回帰）も再実行して合格。

### 9.9-5. まだ手を付けていない選択肢

通知を `interruptionLevel = .timeSensitive`（時間指定通知）にすると、
集中モードと「通知の要約」を**突き抜けて**定刻に出せる。休憩タイマーはこの用途の代表例なので筋は良い。
ただし `com.apple.developer.usernotifications.time-sensitive` の entitlement が要り、
Apple Developer 側で Capability を足してプロビジョニングを作り直す必要がある
（＝ Codemagic の署名をやり直すことになる）。
v0.13.5 で原因が特定できてから判断する。

## Phase 9.10 — 計器そのものの信頼性を直す（v0.13.6・2026-08-17）

v0.13.5 のコードを外部AIに読み取り専用でレビューしてもらったところ、**要修正3件**が出た。
いずれも「不具合の計器が嘘をつく」種類のもので、放置すると
実機で診断画面を見ても間違った結論に導かれる。指摘はすべて実コードで裏を取ったうえで直した。

### 9.10-1. 予約の「OK」がOSの受理を意味していなかった（高）

`node_modules/@capacitor/local-notifications/ios/Sources/LocalNotificationsPlugin/LocalNotificationsPlugin.swift`
の `schedule` はこう書かれている。

```swift
center.add(request) { (error: Error?) in
    if let theError = error { call.reject(theError.localizedDescription) }   // 非同期
}
ids.append(request.identifier)
...
call.resolve(["notifications": ret])                                          // ← 先に返る
```

つまり **`schedule()` の resolve は「注文票を渡した」までで、OSが受理した証拠ではない**。
さらに悪いことに、add が失敗したときの `call.reject` は
**すでに resolve 済みの呼び出しに対する空振り**なので、**JS側には失敗が一切届かない**。
（レビューの指摘は「resolve が先に返る」までだったが、実際にはエラーが握り潰される点まで問題だった）

加えて、旧コードは resolve 直後に `getPending()` を読んでいたため、
add 完了前の一覧を引いて「0件」を掴む競合もあった。

**直し方**：resolve では「受付」とだけ書き、`getPending()` にその id が現れるまで
**400ms 間隔で最大6回**聞き直す。確認が取れてはじめて `OK …（OS確認済み）` と書く。
結果は4通りに分けて表示する。

| 表示 | 意味 |
|---|---|
| `OK … （OS確認済み）` | OSの予約一覧に入っている。ここから先が出ないなら配信・表示側の問題 |
| `NG: OSに予約が入っていない …` | OSが受理しなかった。**旧コードではこれが `OK` に見えていた** |
| `受付 …（確認できず: 理由）` | OSに問い合わせること自体ができなかった（「予約なし」とは別物） |
| `受付 …（予定時刻を過ぎたため確認できず）` | 確認中に予定時刻が来た。消えていて当然なので失敗とは呼ばない |

**ネイティブ側で add の完了結果を直接取る案は採らなかった**。
Capacitor 内部の `add` 呼び出しには外から割り込めないため、
自前で予約経路をもう1本作ることになり、ビルド直前に本番経路を増やす risk のほうが大きい。
`getPending()` による確認は「add が失敗した」という**結果**を確実に捉えられる
（取れないのは失敗理由の文字列だけ）。

**副産物として競合を1つ潰した**：予約は「まず出す → 許可が取れたら出し直す」の2段構えで、
各段が数百ミリ秒の確認を伴う。素朴に書くと先に始まった確認が後から返って、
より新しく確定した情報（例：「通知が許可されていない」）を上書きする。
実際 `denied` のテストで `NG: 通知が許可されていない` が `OK` に上書きされた。
書き手に通し番号（`schedSeq`）を配り、追い越された書き手は黙るようにした。

### 9.10-2. 前面の通知音を切っていることが画面に出ていなかった（高）

`capacitor.config.json` の `presentationOptions` は `["banner", "list", "badge"]` で、
`"sound"` を**意図的に**外している（前面ではアプリ内アラームが鳴るため、二重に聞こえるのを避ける）。
ただし Capacitor は配列を明示すると**書いていないものを明示的に抑止する**ので、
診断がぜんぶ正常でも前面通知は無音になる。これは故障に見える。

**直し方**：診断に **「アプリを開いている時の通知」** 欄を新設し、
`バナー・通知センター・バッジ / 通知音オフ（アプリ内アラームと二重に鳴らないようアプリ側で切っています）`
と書き出す。設定値はJSからは読めないので `app.js` の `FOREGROUND_PRESENTATION` に写しを持つ。
**片方だけ変えると診断が嘘になるので、変更時は `capacitor.config.json` と必ず両方直すこと**（両方にコメントあり）。

なお `presentationOptions` は前面表示だけを決めるため、
**背面で通知が出ないことの説明にはならない**（この点はレビューの指摘どおり）。

### 9.10-3. 診断の取得に失敗すると古い正常値が残っていた（中）

`fillDiag()` は問い合わせが失敗しても「答えた」扱いにして、値を更新せずに再表示していた。
そのため前回 `granted` だった欄は、今回取得に失敗しても **`granted` のまま**出ていた。
権限まわりを疑っている最中にこれは致命的。

**直し方**：`fillDiag(sel, key, work)` に変え、失敗時は `soundDiag[key]` を `NG: 理由` で、
時間切れ時は `確認が返ってきません（時間切れ）` で**必ず上書き**してから表示する。

### 9.10-4. 検証

`check_notif_diag.py` を13モードに拡張して全合格。今回追加した肝は次の3モード。

| モード | 再現する状況 | 期待 | 結果 |
|---|---|---|---|
| `addlate` | **実機と同じ**。`schedule()` が先に resolve し、OSへの登録は900ms後に完了 | `OK（OS確認済み）` | ✅ |
| `lost` | `schedule()` は成功するがOSに入らない（＝ add 失敗） | `NG: OSに予約が入っていない` | ✅ **旧コードでは `OK` と出ていた** |
| `pendfail` | OSへの問い合わせ自体が失敗 | `受付…（確認できず: 理由）` かつ「OS予約」は `0件` と区別 | ✅ |
| `permfail` | 1回目は `granted`、その後取得できなくなる | 2回目に `NG: settings unavailable` | ✅ **旧コードでは `granted` が残っていた** |

全13モードで「前面の通知音オフ」が表示されること、
`denied` で `NG: 通知が許可されていない` が上書きされないことも合わせて検証。
`check_warn.py`（9モード）・`check_finish.py`・`smoke_kintore.py` も再実行して合格。
`node --check js/app.js` → `node build-ios.js` → `npx cap sync ios` まで通っている。

### 9.10-5. この版で分かること・分からないこと

v0.13.6 は**原因を特定するための版**であって、通知が出ない原因を直した版ではない。
実機で「設定 > タイマー > 音の状態」を見れば、次のように切り分けられる。

| 通知の予約 | OSに入っている予約 | 配信された通知 | 読み方 |
|---|---|---|---|
| `NG: OSに予約が入っていない` | `0件` | — | **OSが予約を受理していない**。ここが本丸 |
| `OK（OS確認済み）` | `1件` | `0件` | 予約は入ったが**配信されていない**（許可・集中モード・要約） |
| `OK（OS確認済み）` | `1件` | `1件以上` | 配信はされている。**画面に出ていないだけ**（要約・表示設定） |
| `NG: 同じIDの古い予約が残っている` | `1件`（時刻が違う） | — | 予約は在るが**今回の時刻に置き換わっていない**（v0.13.7 で見えるようになった） |
| `受付（確認できず: …）` | `NG: …` | — | OSに聞けていない。プラグイン側の異常 |

## Phase 9.11 — 計器の残りの嘘を潰す（v0.13.7・2026-08-17）

v0.13.6 を同じ外部AIに再レビューしてもらい、**要修正2件**が出た。
どちらも 9.10 と同じ「計器が嘘をつく」種類のもので、実コードで裏を取って直した。
アプリの動きそのもの（通知の出し方・音・振動・UI）は一切変えていない。

### 9.11-1. 予約の確認が id しか見ていなかった（高）

v0.13.6 の `confirmScheduled()` は、`getPending()` の一覧に**同じidがあれば**
`found`（＝`OK（OS確認済み）`）としていた。
だがこのアプリは**常に同じidで予約を置き換える**作りなので（9.8-4）、
前回の予約が残っているだけでもidは一致してしまう。
つまり「置き換えに失敗して古い時刻の予約が残っている」状態を、正常として表示していた。

そうなり得る操作は日常的に起きるものばかりで、実機での誤判定に直結する。

- タイマーの **＋30秒／−30秒**
- **一時停止からの再開**
- **実行中の通知音の変更**
- **通知テストの連打**

**直し方**：idに加えて予約時刻も突き合わせる。

```js
var t = pendingAtMs(n);
if (t === null) return { state: 'found', notif: n };                    // 時刻が読めない版は id 一致で足りたことにする
if (Math.abs(t - atMs) <= PENDING_AT_TOLERANCE_MS) return { state: 'found', notif: n };
stale = { at: t, notif: n };                                            // idは合うが時刻が違う＝置き換え待ち
```

判定は4つから5つに増えた。

| 返り値 | 意味 | 表示 |
|---|---|---|
| `found` | 今回の時刻で入っている | `OK …（OS確認済み）` |
| `stale` | **同じidの古い予約が残ったまま** | `NG: 同じIDの古い予約が残っている（OS hh:mm:ss → 今回 hh:mm:ss）` |
| `missing` | そもそも入っていない | `NG: OSに予約が入っていない …` |
| `fired` | 確認中に予定時刻を過ぎた | `受付 …（予定時刻を過ぎたため確認できず）` |
| `error` | OSに聞けなかった | `受付 …（確認できず: 理由）` |

**許容誤差を 1秒にした理由**：`getPending()` が返す時刻は
`LocalNotificationsHandler.makePendingNotificationRequestJSObject` が
`ISO8601DateFormatter()` で文字列にしたもので、**ミリ秒が落ちて秒単位に丸まる**。
丸めの分だけ必ずずれるので、ここを厳密一致にすると正常な予約まで `stale` になる。

```swift
if let date = schedule["at"] as? Date {
    let dateString = ISO8601DateFormatter().string(from: date)   // ← 小数秒なし
    schedule["at"] = dateString
}
```

**時刻が読めないときは id 一致で `found` にする**のも意図的な判断。
判定できないものを NG 扱いにすると、正しく予約できているのに NG と出す計器になり、
「計器が嘘をつく」問題を別の形で再生産することになる。

**`stale` を見つけたら `fired` より優先する**。予定時刻を過ぎたなら消えていて当然だが、
古い予約が居座っているならそれでは説明できないので、最後まで置き換わるか見る。

**副産物**：`writeSched()` が「書けたか」を返すようにして、
追い越された確認は内部エラーの記録もやめさせた。
これが無いと、＋30秒のたびに古い確認が「置き換わっていない」と判断し、
実害のない失敗が内部エラー欄に溜まる。

### 9.11-2. 診断の欄を古い問い合わせが上書きできた（中）

v0.13.6 の `fillDiag()` は、失敗と時間切れで値を潰すようにはなったが、
**同じ欄に問い合わせが複数飛んだときの順序**を見ていなかった。
診断の問い合わせは「設定画面を開く」「タイマーを開始する」「他アプリから前面に戻る」の
どれでも走るので、重なるのはむしろ普通の状態だった。

こうなる：

1. 問い合わせAが返ってこない
2. あとから始まった問い合わせBが成功して `granted` を表示する
3. **4秒後、古いAの時間切れが `確認が返ってきません（時間切れ）` で上書きする**

さらに、Aが時間切れになったあとネイティブ側が遅れて成功すると、
`soundDiag` だけが書き換わって画面と食い違う状態も作れた。

**直し方**：欄ごとに通し番号を配り、書き込みと画面反映を1か所に集約した。

```js
var DIAG_TARGET = { perm: '#diagPerm', pending: '#diagPending', /* … */ };
var diagSeq = {};
function claimDiag(key) { diagSeq[key] = (diagSeq[key] || 0) + 1; return diagSeq[key]; }
function writeDiag(key, token, text) {
  if (token !== diagSeq[key]) return false;      // 追い越された＝何もしない
  soundDiag[key] = text;
  var el = $(DIAG_TARGET[key]); if (el) el.textContent = text;   // 値と表示は必ず同時に動く
  return true;
}
```

- `fillDiag()` は開始時に番号を取り、**成功・失敗・時間切れのすべて**で `writeDiag()` を通す
- 問い合わせ役（`work`）は `soundDiag` を触らず、**表示する文言を返すだけ**にした
  （触らせると、追い越された側が表示だけ古い値へ戻せてしまう）
- 時間切れを書けたときは番号をもう一段進めて、**その問い合わせが遅れて返ってきても書けない**ようにした
  （「返ってこなかった」という事実の方を残す）

同じ欄に書く他の3経路も同じ仕組みに載せた。

| 経路 | 書く欄 | 補足 |
|---|---|---|
| `refreshNotifWarning()` | `perm` | 取れなかったときは前の値を書き戻す（元の `\|\| soundDiag.perm` の仕様を維持しつつ、欄が固まらないようにする） |
| `confirmScheduled()` | `pending` | 予約を出した `pushNotif()` が番号を取り、確認ループへ渡す |
| 通知音のコピー3か所 | `files` | `installNotificationSounds` / `copyViaNative` / `copyViaFilesystem` |

`renderSchedDiag()` が `#diagPending` を直接書いていたのもやめた（`writeDiag` に一本化）。
`runNotifTest()` が `refreshPendingDiag()` を呼んでから `renderSoundDiag()` を呼んでいた二重問い合わせも削った。

### 9.11-3. 検証

`check_notif_diag.py` を**15モード**に増やした（`stale` / `stalefix` を追加）。
モックの `getPending` は実機と同じく**時刻を秒へ丸めて**返すようにしたので、
全モードが「許容誤差が効いていること」の確認を兼ねている。

| モード | 何を再現するか | 予約欄 |
|---|---|---|
| `stale` | 同じidの古い予約が残り、置き換わらない | `NG: 同じIDの古い予約が残っている（OS 15:53:13 → 今回 15:54:13）` |
| `stalefix` | 確認している途中で今回の時刻へ置き換わる | `OK …（OS確認済み）` |

**誤検知しないことの確認**も入れた。＋30秒×3 → −30秒 → 一時停止 → 再開 →
実行中に通知音を変更 → 通知テスト連打、と続けても
`OK …（OS確認済み）` のままで、内部エラーは `なし`。

診断の競合は、許可の返事をテスト側が完全に握る専用モック（`RACE_INIT`）で確かめた。

| 手順 | 期待 | 結果 |
|---|---|---|
| A・Bを重ねてBだけ先に答える | `granted` | ✅ |
| そのままAの4秒の時間切れを迎える | `granted` のまま | ✅ |
| そのあとAが遅れて `denied` を返す | `granted` のまま | ✅ |
| Aだけを時間切れにしてから成功を返す | `確認が返ってきません（時間切れ）` のまま | ✅ |

`check_warn.py`（9モード）・`check_finish.py`・`smoke_kintore.py` も再実行して合格。
`node --check js/app.js` → `git diff --check` → `node build-ios.js` → `npx cap sync ios` まで通っている。

### 9.11-4. 変えていないもの

レビューでも「維持すること」と指定された次の点は触っていない。

- `presentationOptions` から `sound` を外している仕様（前面はアプリ内アラームが鳴るので二重にしない）
- 診断の「アプリを開いている時の通知」に**アプリ設定でOFF**と出す表示
- 「まず予約し、許可確認後に同じidで出し直す」流れ
- 通知音ファイル・`AVAudioSession`・振動処理・UI・通知以外の機能

## Phase 9.12 — 診断を映す画面そのものが壊れていた（v0.13.8・2026-08-17）

v0.13.7 を実機で確認したところ、**タイマー画面の見出しのすぐ下に、中身が空の赤い枠**が出ていた。
警告帯 `#trNotifWarn` は「通知が出せない理由があるときだけ出す」ものなので、
空の枠が出ること自体があってはならない。

### 9.12-1. 原因

`index.html` の警告帯は `hidden` 属性を付けて置いてある。

```html
<button class="notif-warn" id="trNotifWarn" type="button" hidden></button>
```

ところが `style.css` 側に `display` を書いていた。

```css
.notif-warn { display: block; width: 100%; ... }
```

ブラウザ既定の `[hidden] { display: none }` は**UAスタイルシート**にあり、
カスケード順で**作者スタイルシートに負ける**。
つまり `hidden` を付けても `display: block` が勝ち、**枠だけが出続けていた**。
`renderNotifWarning(null)` は `innerHTML = ''` にするので、中身のない赤い枠になる。

同じ形の要素をすべて調べたところ、もう1件あった。

| 要素 | クラスの display | hidden が効くか |
|---|---|---|
| `#trNotifWarn` | `.notif-warn { display: block }` | **効かない**（今回の空枠） |
| `#rowIgnoreSilent` | `.s-row { display: flex }` | **効かない**（PWA版でiOS専用の行が見えていた） |
| `#soundDiagSection` | `.s-section`（display 指定なし） | 効く |

### 9.12-2. なぜ気付かなかったか（なぜなぜ5回）

1. **なぜ空の赤枠が出たのか** → `hidden` 属性が効かず、中身が空のまま表示されていた
2. **なぜ効かなかったのか** → `.notif-warn { display: block }` がUAスタイルの `[hidden]{display:none}` を上書きしていた
3. **なぜ検出できなかったのか** → `check_warn.py` が `el.hidden ? null : el.textContent` で判定していた。
   **`el.hidden` は属性が付いているかを返すだけで、見えているかは表さない**。テストは8モードすべてで「隠れている」と報告し続けた
4. **なぜ属性で判定したのか** → このプロジェクトは `display` をCSSで制御するのが常態（`#view-timer.running #timerSetup{display:none}` など）なのに、
   `hidden` 属性と `display` 指定を同じ要素に同居させたときの優先順位を確かめず、「hidden を立てた＝隠れる」を前提にした
5. **根本** → **「意図」を「結果」の代わりに検証した**。
   9.7 は localStorage のコピー済みフラグを「コピーできた証拠」とし、
   9.10 は `schedule()` の resolve を「OSが受理した証拠」とし、
   9.11 は id の一致を「今回の時刻で予約された証拠」とした。今回は同じ誤りが**表示レイヤ**で起きた。
   表示は観測の最終出力なので、ここが嘘をつくと**下流のすべての診断が意味を失う**。

### 9.12-3. 直し方

個別に `.notif-warn[hidden]{display:none}` を足す方式は、`display` を書くたびに書き忘れて同じ事故を繰り返す。
リセット直後で**一度だけ全要素に効かせる**。

```css
[hidden] { display: none !important; }
```

`!important` は詳細度で作者スタイルに勝つために要る（UAより弱いという話ではない）。

テストの判定も**実測へ変えた**。属性ではなく矩形が出ているかを見る。

```js
el.getClientRects().length > 0   // 実際に描画されているか
```

`check_warn.py` に「hidden 属性が実際に効いているか」のセクションを新設し、
①警告が無いときに警告帯が見えないこと ②PWA版でiOS専用の行とセクションが見えないこと を確かめる。

**このテストが検出できることを実証済み**：`www/css/style.css` の `[hidden]` 行を一時的に潰して回すと、

```
× ok: 警告が出るべきでないのに出た（）          ← 空文字列＝実機と同じ症状
× hidden: 警告が無いのに #trNotifWarn が画面に出ている（空の枠）
× hidden: ブラウザ版なのに #rowIgnoreSilent が画面に出ている
```

と落ち、`[hidden]` を戻すと全モード合格に戻る。

### 9.12-4. 空枠から読み取れること・読み取れないこと

警告帯が**空**だったということは、`describeNotifBlock()` が `null` を返していたか、
`renderNotifWarning()` がまだ一度も呼ばれていなかったかのどちらかで、**画面上は区別がつかない**。
`null` になる条件は5つあり、この中には「自分でシステム通知をOFFにしている」も含まれる。

| `null` になる条件 | 意味 |
|---|---|
| `!timerSettings.notifyOn` | **設定でシステム通知をOFFにしている**（このとき通知は予約すらされない） |
| perm が `granted` かつ iOS 設定に問題なし | 通知を妨げる要因は見つかっていない |
| `!ios` | iOS 設定を読めなかった（`AlarmAudio` 未登録など） |
| 未実行 | 初期状態のまま |

**したがって「赤枠が空だから正常」とは読めない**。切り分けは診断の「予約」欄で行う
（システム通知OFFなら `—（システム通知がOFF）` と出る）。

### 9.12-5. あわせて確認した事実：バッジは元からつかない

`buildNotif()` は `id` / `title` / `body` / `schedule` / `sound` しか渡しておらず、
**`badge` を指定していない**。iOS はローカル通知に `badge` が無ければアイコンに数字を出さないので、
**通知が正しく配信されてもバッジはつかない**。
「バッジがつかない」ことは通知が届いていない証拠にはならない。

## ファイル構成

```
ツール_筋トレ記録/
├── DESIGN.md          ← この設計書
├── index.html         ← アプリ本体（SPA）
├── css/style.css      ← 配色（:root＝ダーク / :root[data-theme='light']＝ライト）・モバイルファーストUI
├── js/
│   ├── db.js          ← データ層（localStorage抽象化・CRUD）
│   ├── app.js         ← 画面制御・レンダリング・イベント
│   └── charts.js      ← グラフ・レポート（種目別グラフ・部位別ボリューム、自作SVG）
├── icons/             ← PWAアイコン（192/512/apple-touch-icon）＋ App Store用 icon-1024.png
├── sounds/            ← タイマーのアラーム音WAV5種（アプリ内再生と通知音の両方で使う。tools/gen_alarm_wav.py の生成物）
├── tools/
│   └── gen_alarm_wav.py ← sounds/*.wav を生成する。音を変えるときはJSではなくこれを直す（Phase 9-3）
├── manifest.json      ← ホーム画面追加用
├── sw.js              ← オフラインキャッシュ（HTTPS公開後に有効）
├── gas/code.gs        ← スプレッドシート同期用GAS Web Appテンプレ（ユーザー自身のスプレッドシートにコピペして使う。PWA本体からは読み込まれない）
├── gas/feedback.gs    ← ご意見・ご要望 受付用GAS Web Appテンプレ（受付専用スプレッドシートに貼る。発行URLはapp.jsのFEEDBACK_GAS_URLに埋め込む）
│
│  ===== ここから下は App Store版（Phase 7）専用 =====
├── privacy-policy.html ← プライバシーポリシー（GitHub Pagesで配信）
├── support.html        ← サポートページ・FAQ（同上）
├── package.json        ← Capacitor の依存とビルドスクリプト
├── capacitor.config.json ← appId: com.kanaeru.kintore / webDir: www
├── build-ios.js        ← 上のPWAソースから www/ を生成（クラウド同期をスタブに差し替え）
├── codemagic.yaml      ← Codemagic（macOSクラウド）でのiOSビルド定義
├── www/                ← build-ios.js の生成物（追跡しない）
└── ios/                ← Xcodeプロジェクト（cap add ios で生成、コミット対象）
    └── App/App/
        ├── Info.plist              ← 表示名・縦向き固定・輸出コンプライアンス
        ├── AppDelegate.swift       ← AVAudioSession設定＋AlarmAudioプラグイン＋MainViewController（Phase 9-4）
        ├── Base.lproj/Main.storyboard ← customClass=MainViewController（戻すとプラグインが消える）
        └── Assets.xcassets/        ← AppIcon（1024）・Splash（2732）
```

## データモデル（localStorage キー: `kintore_v1`）

```js
{
  version: 2,
  exercises: [ { id, name, part, equip,       // part: 胸/背中/脚/肩/腕/腹/有酸素/その他
                 video, note } ],             // equip: バーベル/ダンベル/マシン/ケーブル/自重/''（なし）
                                              // video: 参考動画URL（任意）/ note: フォームメモ（任意）
  workouts: {
    "2026-07-08": {
      date: "2026-07-08",
      condition: 5,                            // 1〜5（null=未設定）
      memo: "調子よし",
      entries: [
        { id, exId, name, part, equip,         // name/part/equip は記録時点の値を保持（種目削除後も履歴が壊れない）
          sets: [ { w: 60, r: 10 } ] }         // 筋トレ: { w:重量kg, r:回数 }
                                               // 有酸素(part==='有酸素'): { t:分, ts:秒(0-59), d:km, sp:km/h, inc:%, cal:kcal, hr:bpm,
                                               //   z:強度タグ 'hi'=WORK / 'rec'=REST / ''=なし（v0.10.0追加。
                                               //   zだけが付いた空セッションは「未入力」扱いのまま＝実施済みとは数えない） }
      ]
    }
  },
  dirtyDates: { "2026-07-13": true },            // クラウド同期の送信待ち日付（同期成功で削除。v0.6.0追加）
  dirtyExercises: false,                         // 種目マスタに未送信の変更があるか（v0.8.3追加）
  deletedExercises: [ { part, name, equip } ]    // 削除した種目の控え＝tombstone（v0.9.3追加）
                                                 // シートへの反映はマージ方式のため、削除は明示的に送る必要がある。
                                                 // 送信成功でクリア。同じ種目を再登録・復元すると取り消される
}
```

- マイグレーション：`db.js` の `migrate()` が version を見て自動で引き上げる（v1→v2: equip付与・有酸素種目追加）。`ts`（秒）フィールドはv0.3.1で追加したが、既存データに無くても`cp()`のnullish処理で自動的に空扱いになるためマイグレーション不要。`dirtyDates`はversionに依存せず`load()`内で「フィールドが無ければ既存の全記録日をdirty化して追加」する形で後方互換対応（v0.6.0）
- CSV列（18列・v0.10.0）：`日付,曜日,部位,種目,器具,セット,重量kg,回数,ボリュームkg,時間min,時間秒,距離km,速度kmh,傾斜%,カロリーkcal,心拍bpm,メモ,強度`（筋トレ行は有酸素8列が空、有酸素行は重量/回数/ボリュームが空）。`強度`はv0.10.0で**末尾に**追加（途中挿入だと既存スプレッドシートの列がずれるため）。17列の旧CSVも取り込める

## UIデザイン

- テーマ：ダーク（ジム・鉄の質感）＋ボルトイエロー `#d7ff3e` アクセント。**v0.12.0でライト（ペーパー × ボルト）を追加**し、設定 > 外観で システム/ライト/ダーク を選べる（設計は Phase 8 を参照。**ボルト色を使うときは塗り＝`--volt` / 文字・線＝`--accent` の使い分けを守ること**）
- 数字フォント：Bebas Neue（Google Fonts・オフライン時はシステムフォントにフォールバック）
- 本文：日本語システムフォント（Hiragino / Yu Gothic）
- 下部タブバー：記録・履歴・グラフ・タイマー・計算機・設定（すべて実装済み）
- 入力：±ステッパー（重量は設定した刻み幅=既定2.5kg、回数±1）＋ドラムロールピッカー（重量0.25kg刻み・回数0〜50）＋直接入力（数値キーボード）
- 有酸素の数値欄（距離・速度・傾斜・カロリー・心拍）：タップでキャレットは常に末尾／値があるときだけ✕クリアボタンを表示（v0.10.0）
- 強度チップ：WORK＝ボルトイエロー塗り、REST＝ブルー`#4a7fd4`の輪郭、なし＝`—`のグレー。インターバルのある種目は1行＝1本のラップ行表示に切り替わる（v0.10.0）
- インターバル生成シートの数値欄（分・秒・本数）：1〜2桁で丸ごと打ち直す欄なので、タップすると**全選択**になりそのまま上書きできる（v0.10.1）。上の有酸素の数値欄とは意図的にふるまいを変えている
- 履歴タブ先頭のワークアウトデイ（v0.11.0）：実施日は部位色で塗る／記録はあるが数値未入力の日は塗らずに小さなドット／選択日はパネル色の隙間を挟んだボルトイエローのリング。見出し右の「ワークアウトデイ ▾」で開閉する

## 実装ノート・制約

- **iOSのPWA制約**：画面ロック中のタイマー通知音は制約あり → Phase 2でWake Lock（画面スリープ防止）で対策予定
- **Safariのストレージ削除リスク**：ホーム画面追加したPWAは比較的永続だが、保険としてCSVエクスポート＋Phase 4のスプシ同期で二重化
- CSV仕様：`日付,曜日,部位,種目,セット,重量kg,回数,ボリュームkg,コンディション,メモ`（BOM付きUTF-8・CRLF）
- Service Worker登録は `location.protocol` がhttpの時のみ（file://でのローカル確認時はスキップ）
- ローカル確認方法：`index.html` をダブルクリックしてブラウザで開く（データはそのブラウザ内に保存される）
- **アラーム音を変えるときは `tools/gen_alarm_wav.py` を直して叩き直し、`ALARM_ASSET_VERSION` も上げる**（上げないと端末の `Library/Sounds/` に残った古いWAVが通知音として使われ続ける。Phase 9-3）
- **`sw.js` の `CACHE` は必ず上げる**（現行 `kintore-v46`）。上げないと古い資産が配られる
- **`navigator.vibrate` は iOS では存在しない**。ネイティブ版の振動は `@capacitor/haptics`（前面時のみ）、閉じているときは通知側に従う（Phase 9-3）

## 次のアクション

0. ~~**v0.10.0のリリース手順**~~ → **完了**（2026-08-09。GASをv4に再デプロイ（`clasp redeploy` でデプロイIDを維持＝URL不変）してから、アプリをGitHub Pagesへpushした）。v0.10.1以降はCSV形式が変わらないためGASの再デプロイ不要、アプリのpushだけでよい。iPhoneではホーム画面アプリを開き直して設定画面下部が **v0.11.1** になっていることを確認する
1. ~~iPhoneを新URLへ移行~~ / ~~旧リポジトリ `ChihiroHonma/kintore-log` を非公開化~~ → **どちらも完了**（旧リポジトリが `PRIVATE` になっていることを2026-08-07に `gh repo view` で確認）
2. **Phase 7：App Store公開** → **コード側は完了（2026-08-10）。上記「Phase 7」を参照**
   - 方針確定：**v1.0＝クラウド同期なし**（iCloudバックアップ＋ローカル通知）で確実に審査を通す → **v1.1＝Googleログイン同期**を追加。PWA版は自分専用として現状維持
   - 完了：実装・自動検証・プライバシー/サポートページ・`codemagic.yaml`・Xcodeプロジェクト・アイコン（7-8の1〜9）
   - 完了：アプリ名決定＋App Store Connectでアプリ作成（`筋トレLog` / Apple ID `6800222286`）、スクリーンショット6枚×2サイズ（7-13）、**Codemagicビルド成功＋App Store Connectへアップロード**（2026-08-11 / Index 4 / 7-14）
   - 完了：TestFlight配信（内部テスト「社内テスト」にテスター招待済み。1.0(4)/1.0(5) が配信可能）
   - **次にやること**
     1. **Codemagicで v0.13.2（Phase 9.6の通知音）を再ビルド → TestFlightで実機検証**
        - v0.13.0 で確認済み：アプリを**開いている**ときのアラーム音は**鳴る** ✅
        - v0.13.1 で確認済み：アプリを**閉じている**ときも**通知バナーが出る** ✅（2026-08-11）
        - **未解決**：その通知に**音が付かない**（バナーのみ） ← Phase 9.6 で対策済み・実機確認待ち
        - 見るところ：**設定 > タイマー設定 > 音の状態**の**「通知音の実ファイル」**（`アプリ本体に5本` と出るか）と**「iPhone側の通知設定」**（`音オン` か）。組み合わせで原因が決まる（9.6-4の表）
        - 「通知テスト」の「試す」を押してホーム画面に戻り、10秒後に**音が鳴るか**
        - **バイブレーション**が振動するか（前面時）
        - 「消音モードでも鳴らす」をOFFにすると消音スイッチに従うこと
     2. 同上 — ライトモードで**ステータスバーの時刻・電池が読めるか**（8-6の唯一の未確定項目）
     3. 同上 — 機種変更時の復元（`Documents/`からの自動復元。シミュレータでは確認済み）
     4. App Store Connect でメタデータ入力（文言は7-12に確定済み）・スクショアップロード・App Privacy申告・審査提出
3. **Phase 8：ライトモード** → 実装・自動検証は完了（2026-08-10 / v0.12.0）。**実機で確認すること**
   - iPhoneでライトにしたとき、**ステータスバーの時刻・電池が読めるか**（8-6の未確定事項。読めなければ対処が要る）
   - ホーム画面のPWAを開き直して設定画面下部が **v0.12.0** になっていること
3. Phase 2 以降へ
