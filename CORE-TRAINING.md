# 体幹機能・ビルドの分離

- 基準: `ee3767ad15b3fe2b39d1f295100ddc56806fda4c`（iOS 1.0.3、Web 0.13.21）。2026-09-23にGitHubの `ios/assist-keyboard-controls` と一致を確認。
- 変更前タグ: `ios-pre-core-20260923`
- 開発ブランチ: `feature/core-training`
- 体幹版: iOS 1.1.0、Web 0.14.1。iOSビルド番号はCodemagic実行時の `BUILD_NUMBER`。
- iOS専用ワークフロー: `ios-core-preview`。このブランチ以外では停止し、署名IPAとコミット情報を成果物にする。App Store Connectへのアップロード・公開は行わない。
- 元のmain作業フォルダーと最新iOS作業フォルダーは変更しない。

## 記録仕様

「体幹」を追加し、時間・回数・保持時間×回数の3方式に対応する。初期種目はプランク、サイドプランク、ドローイン、デッドバグ、バードドッグ、パロフプレス。器具の選択肢からチューブ・バランスボールを削除。

左右を分けず、左端のセット番号と入力欄を1行に表示する。保持時間×回数は両方入力した行だけを集計し、未入力を表示する。秒数・回数は0～86400の整数。空欄・0は未実施。新しい日の値・追加セットは空欄とし、意図的な複製ボタンだけで値をコピーする。

種目マスターと日々の記録の両方に方式を保存する。方式を変更しても過去と入力中の記録には適用しない。既存「腹」のプランクは自動移動せず保持する。

重量・ボリューム・1RMからは除外し、体幹時間・回数を有酸素と分けて集計する。グラフは実施日数、合計時間、最長保持時間、回数、セット数。休憩時間の記録欄は追加しない。当日のメモは既存の日メモを使う。

## データと復元

localStorageキー `kintore_v1`、アプリ識別子、WebViewの保存先は維持する。既存記録・種目IDを変えず、初期体幹種目と `coreSchema: 1` を追加する。初期種目の重複追加を防ぐ。

変更前データは初回移行時に `kintore_v1_pre_core_20260923` へ保存する。コピー失敗時に既存データを初期化しない。iOSでは通常バックアップ更新の前に、Documentsの `kintore-pre-core-20260923.json` も作成し、存在する場合は上書きしない。設定の「体幹追加前のデータ」からも保存可能（ブラウザはJSONダウンロード、iOSはDocumentsへの保存）。

CSVは従来21列を保持し、方式・左右別・左/共通秒・左/共通回数・右秒・右回数の6列を追加。18列・21列の既存CSVも読み込める。変更前のアプリは体幹入りCSVを理解しないため、体幹入りデータを旧アプリへ直接読み込ませない。

### コードを戻す

未コミット変更のある作業フォルダーをリセットせず、新規フォルダーに変更前タグから作成する。

```powershell
git worktree add --detach ../kintore-before-core ios-pre-core-20260923
```

変更前フォルダーで依存をインストールしビルドする。Apple配布用のビルド番号は既存番号を再利用せず、新しい番号を指定する。タグで戻るのはコードであり、端末データではない。

### 端末データを戻す場合

1. 体幹版で増えた記録をCSVと現行JSONに退避する。
2. 保存済みの変更前JSONと、使用端末の復元方法を確認する。
3. 旧コードを動かす前に、変更前JSONをその端末の保存領域・通常Documentsバックアップへ復元する。
4. 種目数・日付・主要記録を読み返す。

JSONの全置換は旧版の一般UIにはないため、開発者による端末復元作業が必要。アプリを削除・再インストールして戻すことはしない。移行後の記録を維持したまま旧版へ戻す場合は、別途データの変換設計が必要。

## 検証

`npm run verify` で構文・セキュリティ・既存CSV/JSON復元・更新時データ保持・AlarmKit・CSV保存完了通知・体幹の回帰チェック・iOS向けWeb資材生成を行う。

体幹回帰チェックは3方式、左右/片側、空欄、値の範囲、セット複製、履歴スナップショット、CSV/JSON往復、ボリューム/グラフ分離、移行の冪等性、バックアップ容量不足時の非破壊を含む。

ブラウザでは390px幅・デスクトップ幅、ライト/ダーク、種目追加の2経路、記録入力、セット追加/削除、再読み込み、履歴、グラフ、CSV/変更前JSONのダウンロードを確認。

WindowsではiOS署名コンパイルは実行できない。IPA生成、TestFlight、実機のキーボード・ファイル保存・更新/復元は別途検証する。

## B案の数値入力（Web 0.14.1）

- 体幹の直接入力ではキーボード上に対象と「完了」を表示する。
- 筋トレの重量・回数を手入力すると、ホイールと「少ない・多い」を隠す。入力欄の下にキャンセル・対象・完了を配置する。
- 完了で値を確定し、キャンセルでは元の値を維持する。入力中は下部ナビゲーションを隠す。
- 変更直前のコードはローカルタグ `pre-input-b-20260923`（`3a16fb9`）。
- 初回移行時に `kintore_v1_pre_input_b_20260923` へ変更前データをコピーし、`coreSingleSchema: 1` で再移行を防ぐ。
- 既存の左右別記録は入力済みの左右をそれぞれ独立した行へ変換するため、セット数は増える場合がある。元の行は記録内の `legacyCoreSets` にも保持する。左右の値は合算・平均しない。
- 廃止器具の既存指定は器具なしへ変更し、元の名称を `legacyEquip` に保持する。
- `tools/input-b-check.js` で移行と再読込を検証。ブラウザで直接入力・キャンセル・小数重量・画面幅・キーボード領域の模擬チェックを実施。iPhoneの実キーボードはTestFlightで別途確認する。
